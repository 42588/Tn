- **核心资产**：`calm-glass.css` 作为单一样式源（Single Source of Truth），定义 CSS 变量（颜色、圆角、噪点滤镜）及组件样式。
- **原型结构**：`mockup.html` 为全窗口合成真源，用于守卫 Token 一致性；`panels/` 目录拆解为 5 个独立面板原型（窗口外壳、工作区、侧栏、浮层、状态屏）。
- **视觉映射**：样式表注释明确指向 `docs/样式还原手册.md` 及 GPUI 实现细节，确保 Web 原型到原生渲染的数值对齐。
- **资源配套**：`shots/` 目录提供对应的 PNG 视觉快照，辅助设计评审。
# 快速预览编辑器近期变更

## 定位

本文记录 Tn Editor / Quick Look 自绘编辑器在 2026-06 的近期变更、修复、性能守卫和待验事项。发布摘要见 [CHANGELOG.md](../../CHANGELOG.md)。

## 背景

本轮围绕 Quick Look 编辑器从 `uniform_list` 逐步迁移到自绘 File/Edit/Diff 路径,同时把纯文本编辑核心下沉到 `tn-editor` headless crate。CHANGELOG 只保留面向发布的摘要,实现细节沉到本文。

## 具体内容

### headless 编辑核心

- 新增 `tn-editor` workspace crate,无 GPUI 依赖。
- 迁移 Quick Look 的 `char_to_byte`、`line_chars`、`insert`、`newline`、`backspace`、`delete`、`move`、`page`、`delete-range`、`insert-multiline` 等纯文本编辑函数。
- 新增 `Document`、`Selection`、`CursorSet`、`EditTransaction`、`UndoStack`、`SearchState` headless 模型和独立单测。
- `tn-ui::quick_look` 编辑态通过 `QuickLookEditState` 薄壳把打字、选区、剪切/粘贴、查找/替换、撤销/重做和保存接入 `Document` 主状态。
- 旧 `uniform_list` renderer 暂继续读取适配后的 `Rc<Vec<String>>` 快照,为 LineLayout / EditorElement 上移打基础,用户可见行为保持不变。

### 自绘 File / Edit 路径

- `TN_QL_ELEMENT=1` 时启用自绘只读 File 预览:新增 `paint_file_preview` 与 `QuickLook::file_element`。
- File 只读预览改走 GPUI `canvas` 自绘,包含行号右对齐、语法着色文本和横滚 thumb。
- 使用 `editor::{geometry,prepaint}` 模型按 1/2 列网格逐段 `shape_line` + `paint`,ASCII 连排,CJK 单字 2 列步进,固定单元格避免漂移。
- 纵向滚动由 `el_scroll_y` 驱动并 clamp;横向复用 `hscroll_px`;文本通过 `with_content_mask` 裁到 gutter 右侧。
- 自绘 File 预览新增只读选区、复制、CJK 命中、Shift+滚轮 / 触控板横向滚动和横滚条 thumb 拖拽。
- `file_element` 鼠标 down/move/up 通过 y 反推行号,通过 `caret_col_at_x` / `hover_char_at_x` 计算列,并处理 CJK 双宽与 `pressed_button` 兜底。
- `Ctrl+C` / `Ctrl+A` 复用既有 `place_cursor`、`copy`、`select_all`。
- 自绘 File 预览/编辑器已成为默认路径;`TN_QL_LEGACY=1` 强制回退旧 `uniform_list`,Diff 仍保留旧路作为逃生口。
- TnE-11 让 `file_element` 同时承载预览 + 编辑;编辑态行源走 `edit` 镜像,避免每帧深拷。
- `paint_file_preview` 加 `editing` / `caret` / `ime`:绘制瞬时反相块 caret、选区底色、IME preedit 和 accent 下划线。
- editing 时 `window.handle_input` 注册输入处理器,支持中文合成和 WM_CHAR。
- 新增 `el_follow_caret`,仅在 cursor 变化时跟随并做纵横滚动去抖。

### 查找与 IME parity

- `find_next` 在 `el_render` 下走 `el_center_row`,命中行滚到视口纵向居中,并 pin `last_follow_cursor`,避免 render 顶部的 `el_follow_caret` 把它弹回边缘。
- `file_element` 实时计算 `all_matches(query)`,`paint_file_preview` 逐行绘制全部命中底色。
- 命中高亮从 `cola(accent_alt,0.20)` 纯填充升级为 `gpui::quad`,使用更醒目的填充、描边和圆角。
- `find_next` 新增 `el_reveal_col`,命中列超出视口时将 `hscroll_px` 横向居中到该列。
- 查找框中文输入修复:查找打开时注册 IME handler;`find_key` 只接管 Esc/Enter/Tab/Backspace,可打印键放行给 IME handler。
- `replace_text_in_range` 在 `find_open` 时把提交/合成文本路由进 `find_query` / `replace_query`。
- 查找条 active 字段回显 `ime_marked` preedit,正文不重复画。
- IME 候选框定位修复:新增 `find_field_bounds`,find_bar 激活字段输入框挂占位 `canvas` 每帧写入窗口坐标;`bounds_for_range` 在 `find_open` 时把候选框对齐到输入框左下缘。
- 正文编辑 IME 候选框定位修复:正文 `bounds_for_range` 改用与绘制层同源的 `quicklook_caret_paint_rect`,候选框跟随真实 caret 底部,覆盖软换行、CJK 双宽、横滚和纵滚偏移。

### Diff / LineLayout / 配置模型

- 新增 `tn-ui::editor::diff` 装饰/导航模型。
- `DiffRowKind{HunkHeader|Addition|Deletion|Context|Meta}` 提供 `gutter()` / `is_content()`。
- `classify_diff_line` 区分 `+++` / `---` 文件头、增删内容、git meta、mode、rename、二进制和 no-newline。
- 新增 hunk 跳转 `hunk_header_rows`、`next_hunk`、`prev_hunk`。
- 只读 Diff renderer 接入计划为按 `DiffRowKind` 上色并复用 prepaint;本轮不做 accept/reject,真机对照留待后续。
- 新增 `tn-config [editor]` 段与 motion 降级策略。
- `EditorAnimations{Off|Subtle|Full}` 默认 `subtle`。
- `Editor::effective_motion(reduced_motion, high_load)` 在 `off`、OS 减少动态效果或高渲染负载时降级为 `Instant`,否则透传 `subtle` / `full`。
- 默认 `config/config.toml` 加 `[editor] animations` 段。
- 新增 `tn-editor::line_layout` logical -> visual 映射。
- `WrapMode{None | Word{width_cols}}`、`VisualLine`、`LineLayout::build` 支持词换行、硬断、超宽单字独占、CJK=2 列和空行保一条。
- 提供 `logical_to_visual`、`visual_to_logical`、`range_segments`、`hit_test`。
- TnE-16 已把 `LineLayout` 接入 Quick Look File/Edit 自绘路径:Markdown/txt/log/说明文档使用 visual line 软换行,选区/查找命中投影到视觉段,复制和保存仍按逻辑文本坐标;代码文件继续横向滚动。
- 软换行编辑态键盘选择补齐:Markdown/txt/log 这类 `WrapMode::Word` 文件中,`Up/Down` 先按 `LineLayout` 视觉行移动,`Shift+Up/Down` 可在同一长逻辑行的相邻视觉行间扩展选区;代码文件仍保持逻辑行移动和横滚。
- TnE-18 已把轻量 typing effects 接入 Quick Look 编辑态:新增 `tn-ui::editor::motion` 纯模型,同一视觉行小跨度打字/删除使用与终端一致的 cursor glide、块光标 squash/pop。光标运动模型已从固定时长线性偏移调整为终端同款 chase:保留当前绘制列,每帧按 0.4 factor 追逐最新目标列,连续输入时从 in-flight 绘制位置继续追。Quick Look 自绘光标同步收敛为终端同款 1px 圆角,并以 `CODE_FS + 4px` 作为视觉高度居中到 `ROW_H`;这只改变绘制块,不改变行高、IME 锚点、命中测试、selection 或 copy 语义。`[editor] animations=off`、Windows reduced-motion、高负载/4000 行、大文件、IME 合成、拖选、选区、普通方向移动、滚动和结构性编辑均 snap。实现使用单个 16ms frame driver,不积累 per-char 队列,不做字符 settle / afterglow。
- 连续输入卡顿回归修复:自绘编辑器的 `replace_text_in_range` 不再每次文本提交后强制 `scroll_to_item(cursor, Center)`,而是交给已去抖的 `el_follow_caret` 只在光标离开视口时跟随;legacy `TN_QL_LEGACY=1` 路径仍保留旧居中策略。
- TnE-19 新增 `tn-ui::editor::session::DocumentSession`,Quick Look 编辑态由共享 session 承载,并通过 `EditorHandoff` 支持 `Ctrl+Enter` / “打开为编辑器”升级入口。升级后 Editor Pane 和 Quick Look 共享同一份 buffer、cursor、undo/redo 和 dirty 状态。
- TnE-20 新增 `tn-ui::editor::pane::EditorPane` 与 Workspace `editor_panes` leaf 接入。Editor Pane 初版支持基础输入、撤销/重做、复制/粘贴、本地保存、行列/UTF-8/LF/dirty 状态栏和 dirty close prompt;终端 pane、Explorer per-pane cwd 与 agent 状态仍走原 terminal `panes` 表。

### Prepaint 与几何模型

- 新增 `tn-ui::editor::prepaint` 只读渲染模型,产出 `EditorElement::prepaint` 所需布局纯函数。
- 关键函数包括 `visible_row_indices`、`row_top`、`gutter_label`、`content_origin_x`、`prepaint_readonly`。
- `ReadOnlyPrepaint` 包含 `content_w`、`max_off`、`h_offset`、`rows`、`thumb`、`content_x`。
- 新增 `tn-ui::editor` 几何模块骨架 `crates/tn-ui/src/editor/`。
- 把 Quick Look `uniform_list` renderer 内联几何复刻成纯函数和数据结构:`Metrics`、`disp_width` / `prefix_cols`、`caret_x`、`content_width`、`max_h_offset`、`hover_char_at_x` / `caret_col_at_x`、`visible_rows` / `row_out_of_view`、`follow_h_offset`、`h_scroll_thumb` / `h_offset_from_drag`、`caret_abs_x`。
- 几何模块为后续 `EditorElement` 的 layout/prepaint 提供 Quick Look / Editor Pane / Diff Review 共享可测模型。
- 几何模块不接入任何 render 路径,保留 `#![allow(dead_code)]` 脚手架;Quick Look 默认渲染与旧 `uniform_list` 不变。

## 修复记录

- 退出编辑回预览显示旧内容:预览从 `file_data` 渲染,编辑只改 `Document` / 编辑缓冲,escape 退出编辑时没有把缓冲镜像回 `file_data`。新增 `QuickLook::sync_preview_from_edit`,dirty 时把编辑缓冲写回 `file_data` 并清高亮缓存,不写盘、不动 dirty。
- OS 关窗绕过 dirty-close 守卫:标题栏关闭 / Alt+F4 曾绕过 `request_quit`,有未保存 Quick Look 编辑时会静默丢失。在 workspace reveal 块注册 `window.on_window_should_close`,Quick Look 打开时返回 `quick_look.request_quit()`。
- 旧编辑 renderer 下线推迟到 TnE-13:旧 `uniform_list` 路径仍被 Diff tab 共用,且是 `TN_QL_LEGACY=1` 紧急逃生口,暂不删除。
- 外部目录变更导致 Quick Look 键盘失效:其它软件修改当前目录文件会触发 workspace / explorer / agent rail 刷新,浮层仍显示但 focus 可能落到底层,导致 `Esc`、`Up` / `Down` 不再进入 Quick Look。修复为 workspace 在 Quick Look 打开且没有更高优先级 overlay 时持续确保 Quick Look focus,并把 Quick Look 纳入 overlay focus 冻结策略。回归测试:`quick_look_open_counts_as_focus_freezing_overlay`。
- 外部编辑器保存后 Quick Look 内容不刷新:Explorer 的目录 watcher 只重建文件树,没有把变更通知给 Quick Look。修复为 explorer debounce 后发 `ExplorerChanged`,workspace 通知 Quick Look 检查当前本地文件;预览态 clean 状态 guard 变化时自动 reload;编辑态不自动 reload、不退出编辑、不立即弹冲突,后续保存继续由 guard 提示冲突。回归测试:`external_reload_decision_refreshes_clean_file_but_preserves_dirty_edit`。
- Editor Pane dirty 关闭只阻止但无提示:Workspace 关闭 pane / tab / app 时只收到 `ConfirmDirty` 后 `notify` 并返回,没有记录待关闭动作,Editor Pane 渲染层也没有可见确认条。修复为 `EditorClosePrompt` 记录 `Pane` / `Tab` / `App` 意图,Editor Pane 内显示“保存 / 放弃 / 取消”;保存或放弃后发 `EditorPaneEvent::CloseConfirmed` 续跑 pane、tab 或 app 关闭。tab / app 会先移除已确认的 editor,再继续检查剩余 dirty editor;标题栏 / Alt+F4 与菜单退出共用同一 guard。回归测试:`dirty_close_request_opens_visible_prompt_state`、`dirty_close_prompt_matches_close_intent`。

### 2026-06-08 迁入条目

- Quick Look 编辑态接入 `tn-editor::Document`:编辑主状态从 UI 层 `buf/cursor/sel_anchor/undo/redo/search` 字段收敛到 `QuickLookEditState` 薄壳内的 `tn-editor::Document`。打字、换行、缩进、退格/删除、撤销/重做、全选、剪切/粘贴、查找/替换、本地/远端保存均通过 `Document` 更新;预览态只读选区仍留在 Quick Look 外层。
- 本地保存守卫与格式保持:本地打开路径复用 `preview_payload_from_bytes`,把 `TextFormat` 与 `FileGuard(mtime/size/hash)` 带回 `QuickLook`。保存前对比当前磁盘 guard,冲突进入「重新载入 / 取消 / 覆盖保存」路径;正常保存保留原编码、换行风格与 final newline。对应单测为 `local_file_guard_hashes_current_file_bytes` 与 `local_save_preserves_format_and_refuses_external_changes`。
- TnE-14 Diff 拖选 / 复制 / hunk 跳转:Diff tab 自绘 renderer 新增选区归一化、跨行复制、CJK + 横滚 hit-test、display-column span、hunk 导航和 diff 行到 new-file 行映射。`diff_element` 接入鼠标 down/move/up,`paint_diff_preview` 绘制选区底色;`Ctrl+C/A` 复用只读复制/全选;`PgUp/PgDn` 跳 hunk;`Enter` 或双击切回 File tab。
- Diff 行回 File 行跳转修正:真机验收发现 hunk 内 `---` / `+++` 正文被误当 metadata 过滤,导致 new-file 行号漂移;删除行也总是向下找存活行。修复为 `parse_diff` 增加 hunk 状态,文件级 metadata 只在 hunk 外过滤;删除行在同一 hunk 内找前后存活 new-file 行,按 diff 距离最近选择且等距偏上一行。`goto_diff_target` 夹住目标行和 `file_jump_highlight`,自绘 File 与 `TN_QL_LEGACY` fallback 都绘制目标高亮。

## 性能守卫

- 复核并钉死编辑核心增量机制:`tn-editor::Document` 的 undo/redo 使用 `EditTransaction` + 行区间 `EditSnapshot`,只拷受影响行,不存整 buffer。
- 连续打字按 `start_row` coalesce 成一条仍行有界的记录,移动光标即断开。
- Quick Look 薄壳 `sync_lines` 仅按 `last_transaction` 的行区间 `splice` 镜像,不再每键 `to_vec()` 整 buffer。
- 新增 `continuous_typing_keeps_undo_records_line_bounded`:4000 行连打 500 字 + 换行,undo 栈合并为 1 条且 before/after 行数 < 8,移动光标后第二条记录仍有界。
- 与既有 `undo_history_does_not_store_full_buffer_for_single_line_edit`、`tn-ui::edit_state_updates_line_mirror_without_replacing_whole_buffer` 合围守卫。

## 验证 / 状态

- `cargo test -p tn-ui --lib` 曾在相关阶段达到 137 / 140 测全绿。
- `cargo test --workspace --lib` 曾在相关阶段全绿,包含 tn-config 42 与 tn-editor 27。
- `cargo build --workspace` 曾在 TnE-11 阶段通过。
- 2026-06-09 TnE-16:`cargo test -p tn-ui --lib` 156 passed;`cargo test -p tn-editor` 27 passed。
- 2026-06-09 Quick Look 外部变更焦点修复:`cargo test -p tn-ui --lib` 157 passed。
- 2026-06-09 Quick Look 外部保存刷新修复:`cargo test -p tn-ui --lib` 158 passed。
- 2026-06-09 TnE-18:`cargo test -p tn-ui --lib` 161 passed。
- 2026-06-09 Quick Look 输入法与软换行选择修复:`cargo test -p tn-ui --lib` 164 passed;新增正文 IME caret rect、软换行视觉行上下移动、输入提交滚动策略回归测试。
- 2026-06-09 TnE-18 终端同款 cursor-only 动画调整:`cargo test -p tn-ui --lib` 168 passed;新增打字/删除动画触发、无字符残影、frame driver active window、连续输入 chase 沿用 in-flight 绘制位置回归测试。
- 2026-06-09 TnE-18 光标视觉几何调整:新增 `self_painted_caret_visual_matches_terminal_radius_and_text_scale` 回归测试;Quick Look 自绘光标和 legacy `cursor_block` 均使用 1px 圆角与字体匹配的视觉高度,IME caret rect 与软换行 selection/copy 测试保持通过。
- 2026-06-09 TnE-19/20:`cargo test -p tn-ui --lib` 177 passed;新增共享 session handoff、EditorPane 状态/dirty prompt 和 workspace editor leaf 插入回归测试。
- 2026-06-09 TnE-21 全局收尾:`cargo test --workspace --lib` 全 workspace 库单测通过,`cargo build --workspace` 通过,Markdown 相对链接检查 77 个文档文件通过,`TN_AUTOQUIT=1 cargo run -p tn-app` 输出 `TN_GUI_OK` 网格并以 0 退出。
- 真机已验自绘只读选区、复制、CJK 命中(2026-06-08 owner)。
- 仍待真机验证:长 Markdown/txt/log 跨视觉行滚动/选区/复制/查找,正文 IME 候选框跟随真实 caret,代码横滚,TnE-18 连打/IME/4000 行/大文件 `TN_PERF`,保存、只读 Diff renderer 对照。

## 反向链接

- 母文档: [修复与优化记录索引](../修复与优化记录索引.md)
- 发布摘要: [CHANGELOG.md](../../CHANGELOG.md)

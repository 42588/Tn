# BUG 发现

跟踪与 triage 见 [docs/任务/2026-06-12-BUG发现清单处理.md](任务/2026-06-12-BUG发现清单处理.md)。

1. ~~ssh 链接后，执行命令并未与宠物联动~~ —— ✅ 已修(2026-06-12):shell open 后立即注入 bash/zsh 自动检测的 OSC 133/633 集成脚本(base64 编码 + stty -echo 隐身注入);宠物 Running/Success/Error 演出完整激活
2. ~~ssh链接后，若意外断开，不会保存之前的快照，默认重新开始工作~~ —— ✅ 已修(2026-06-12):首次连接成功后所有重连状态文字改走 PtyEvent 浮层(B1/B4),不再写入 terminal buffer;终端网格在断开/重连期间保持冻结快照
3. ~~ssh连接后，使用git初始化项目后，无法想Windows一样实时展示文件变更状态~~ —— ✅ 已修(2026-06-12):reader 线程 CommandFinished 设 cmd_done flag;repaint loop 检测 SSH pane → 触发 refresh_changes(agent) / emit FilesChanged(shell);explorer 新增 compute_remote_git_status(SSH 侧 git status --porcelain)

4. ~~quick look展开后出现了面板穿透的问题，我们的在quick look 中进行鼠标滚动等事件的时候，会影响底层的终端~~ —— ✅ 已修(2026-06-12):QuickLook 根节点 + 全部 7 处浮层 scrim 统一吞滚轮
5. ~~宠物交互动画存在一些问题，我们应该重新设计一下鼠标双击事件（不应该是打开设置），鼠标触碰事件（现在是鼠标移动到上面就会闭眼，但是这个闭眼的动作不好看，眼睛直接没了很诡异）然后可以实现一下docs\宠物\小狗家族设计.md下描述的上下文姿态扩展~~ —— ✅ 已修(2026-06-12,经 SHEET 05 板 E 原型审核两轮):双击 = 逗弄玩耍(认可);眯眼 = **方案 A 衬底+下缘横缝**(首版透明洞根因已修);打盹 = **每品种趴姿网格**(真·趴下:腿收、贴地、压扁);探头 = **岗台线后升起**(线下裁切的真·冒出,500ms);决议与参数固化在板 E,结构守卫测试护住网格
   agent中的本次改动栏中的数据不是真实数据，似乎乱了，出现了一些错误数据，有时候我点击查看的时候却显示没有改动，那没有改动你放这里干什么呢
   我想给我们的文件浏览器添加一个拖拽功能，比如我从文件管理器拖动一个目录到我们的agent窗格，我们就可以直接将它添加到我们的agent发送栏，拖动一个文件过来就可以直接它放到我们的agent的发送栏，当我们发送的时候就直接发过去了
   agent面板中的历史记录好像无法通过鼠标滚动上下滑动查看，也不是无法查看，就是只能查看一部分，太长了就看不到了，我记得我们之前设置了一个历史记录本地化的功能还在吗？
   终端项目互联网调研报告中记录了一个AI 终端、编码 agent 与用量监控的项目，你可以学习它如何配置用量信息的，因为我们目前的用量环展示的信息不准确（上下文信息不准确，明明是1M却显示200k,），我也想看到周额度，日额度的信息，但是很明显我们现在的功能无法满足

# 按截图差异修正磷光样式

## 定位

依据 [design/原型与真机截图差异总结.md](../../design/原型与真机截图差异总结.md) 的 P0/P1 清单,对现有 GPUI 实现做样式回归修正。本轮先收掉两个差异最大的 P0 集群:QuickLook 浮层语法与幽灵终端运行态比例,再把需要产品决策的结构性 P0 列为阻塞项交回用户。

## 本轮已改(P0 样式)

### QuickLook(`crates/tn-ui/src/quick_look.rs` + `workspace.rs`)

- 删除浮层左侧贯穿全高的 2px 磷光竖脊 `seam`(原 `quick_look.rs` 6604-6611 + `.child(seam)`)。左脊是 tile/row/命令块的「选中」语义,浮层借用会让磷光退化为装饰边框(总结 P0)。海拔改回 float 投影 + h2 边表达。
- 给 `workspace.rs` 的点击关闭 scrim 补上纯色压暗背景 `bg(rgba(SCRIM))`,让底层终端被压暗,QuickLook 才像「工作区之上的临时速览浮层」。Explorer 仍在 scrim 之外保持可点(点树换预览)。
- 浮层尺寸从「高 88% 撑满 pane」收回到横向速览比例:宽 86%/上限 1080、高 80%/**上限 600**,避免被 pane 可用高度撑成近全屏编辑器(总结 P0:尺寸/外边距)。
- File/Diff 分段开关顺序回到 **File 左 / Diff 右**(原为 Diff/File)。

### 幽灵终端(`crates/tn-ui/src/quick_terminal.rs`)

- 运行态窗体不再用配置的下拉尺寸(默认满屏宽 Top 45%),改为与启动器同一「760 顶垂窗」规格:宽固定 `CARD_W`、水平居中、顶贴屏,仅沿用配置高度(总结 P0:运行态不按屏宽拉伸成横条)。`hidden_for` 统一为从屏顶滑入/滑出。
- 顶缘磷光从「透明→中央亮、右半段满铺」改为左右两段镜像渐变拼出**中央亮、两端渐隐**(gpui `linear_gradient` 只吃两停靠点,故拆两半;总结 P1)。

## 复核未改

- 主 pane 焦点边:`style::plate()` 已是 PH_DIM(·32 dim)边 + 四角角标,符合原型 `.plate.focus`,无需改。

## 验证

- `cargo build -p tn-ui` 通过(本轮全部改动)。
- 真机回归截图待补:QuickLook 居中浮层 + scrim 压暗、幽灵运行态 760 顶垂窗。

## 阻塞:需产品决策的结构性 P0

以下 P0 不是单纯样式,需用户先定权威,再实现或回写原型:

1. 欢迎页是否常驻 Explorer(原型为无 Explorer 整窗 Launchpad)。
2. Agent 是「终端内 CLI」还是「专门 Agent 板面」(决定是否重画 02-workspace-panes)。
3. Agent/Shell 是否恢复 Explorer + Agent + Shell 多 pane 仪表舱。

## 第二轮已改(用户决策后)

用户决策:SSH 过程/TOFU/失败卡片已实现但样式对不上,需对齐;应统一 launch tile 语法。(欢迎页 Explorer 决策见下文「第四轮」——先隐藏后又改回常驻。)

### 欢迎页 Explorer(`workspace.rs`)——先隐藏,后按第四轮决策改回常驻

- 第二轮一度把 Explorer 渲染条件收紧为 `explorer_open && !welcome`(欢迎页隐藏 Explorer);**第四轮用户改主意:欢迎页应常驻 Explorer**,已改回 `explorer_open`,并回写 SHEET 07 板 A/A2(canvas 加 240 Explorer 列)。最终态:欢迎页常驻 Explorer。

### 统一 launch tile 语法(`welcome.rs` + `quick_terminal.rs`)

- 新增共享 `welcome::launch_tile_shape(mono, &CardId, width, selected)` + `launch_glyph_ch`,作为欢迎页/幽灵启动器的**单一** tile 壳:身份字形 mono 600 14、名 sans 600 12 t0、副标 mono 10 t2 大写;统一选中态 = L4 + ph-dim 边 + 左 2px 磷光脊,未选中 = L2 + h0、hover L4 + h1。宽度随面板(欢迎 150 / 幽灵 140),其余一律相同。
- `welcome::card_tile` 与 `quick_terminal::launcher_tile` 都改为复用此壳,磁贴尺寸/图标/选中态不再各自漂移。

### SSH 卡片对齐浮层家族(`terminal_view/mod.rs`)

- 共享 `card_header` 从 34×34 图标徽章头 + 副标题改为标准 `.float-head`:38 高 · L4 顶面 · 底 1px h1 · mono · 彩色字形记号 + 标题 + 右侧身份 chip(原副标信息)。一处改,过程/失败/TOFU/密码/联网五张卡同时对齐(SHEET 06/07)。
- `card_chrome` 宽度 420 → 460(浮层家族 SSH 卡规格);删除 5 处头后冗余手动分隔线(改由 float-head 自带底边)。

验证:`cargo fmt` + `cargo build -p tn-ui` 通过;`cargo test -p tn-ui --lib` 167 passed。

## 第三轮已改(用户决策后)

用户决策:WSL 二级启动器补图纸;SSH 三步进度不改。

### WSL 二级启动器补图纸(`design/panels/07-states.html`)

- 新增板 **A2「WSL 二级启动器」**:定义为 Launchpad 下钻态而非独立子页 —— WSL tile 点击后,同一 Launchpad 把磁贴行换成「返回 tile + 各发行版 tile」,hero/副标/kbd 提示保持原位,与幽灵终端 WSL 下钻(SHEET 04 头部 ‹ 返回)同源。
- 定义**返回 tile**:字形 ‹(t2 中性,不占身份色)· 标题「返回」· 副标「回到启动器 · ESC」· keyboard order 永远第一位(Esc 等效)。
- 定义**发行版 tile**:沿用 `.ltile`(150 宽 · ❯ 字形 · WSL 身份色 = info 蓝 #82B4F0,即 `accent_alt`)· 副标 = 发行版名大写;选中 = L4 + 2px 左脊。
- 定义**状态栏**:从已有工作区进入显示 `N SESSIONS`(磷光实点),区别于新标签欢迎页的 `NO SESSION`(灰点);单发行版时跳过本页直接启动。
- sheet-head em 增列「WSL 二级启动器」;`design/原型与真机截图差异总结.md` WSL 条目标注 ✅ 已补图纸。
- 实现侧 `welcome.rs::back_tile` 命名对齐图纸:`name` 由「‹ 返回」改为「返回」,`sub` 补「· Esc」。
- 浏览器渲染验证通过(playwright 截图:返回/Ubuntu 选中/AlmaLinux-9 + 3 SESSIONS 状态栏)。

### SSH 三步进度严格对齐原型(`terminal_view/mod.rs`)

用户改主意:要严格对齐。把进度卡从竖排标记列表改为原型 SHEET 07 板 C 的**横排** `.steps`:
- dotn 18×18 圆:done = ✓ ok 字 + ok·40 边 + L2 底;busy = ● 磷光填充(ph 底 + ph-ink 字 + ph 边);pending = ○ t3 + h1 边。
- 步间 `.stlink` 连接线 54×1:左侧步已完成 → 点亮 ok·50,否则 h1。
- `.stp` 文字 mono 11:busy = t0,done/pending = t2;busy 步内联认证 detail(`· publickey`)。
- step 标签缩短为原型用词「连接 / 认证 / 打开 shell」;body p16 容器补认证顺序提示行(id_ed25519 → agent → password)。

## Agent 方向已定:中间路 = 既有 Universal Agent Surface

用户确认走中间路;查证后结论:**这就是代码里既有的「Universal Agent Surface」(P4)能力槽设计**,天然 agent-agnostic,无需 per-agent 特例:

- 身份头(`render_agent_header`:amark + 名 + accent)按 registry 命令匹配 descriptor 得出,任何声明 agent 都有。
- 活动栏(`render_activity_rail`)数据源是 `gitutil::changes_for(root)` —— 纯 git/文件,不解析 agent 协议,对任何 agent/shell 一致。
- usage/model/tool_calls/transcript 是能力槽(`AgentCapabilities`):agent 声明则显示,否则优雅缺省。`terminal_only()` = 无适配器 CLI 的基线(terminal + cwd_sync + git_diff)。
- 真机 Codex 像普通终端,是因为它在非 git 的 home 目录跑且未声明 usage → 只剩身份头,是正确的通用缺省,不是 bug。

结论:不实现「专门板面」(会破坏通用性);中间路已由既有架构覆盖。后续如要补,只需让 model/cost chip 在 agent 提供数据时显示,并在 git 仓库内验证活动栏出卡——均不破坏通用性。

## 第四轮:P1/P2 收尾

### QuickLook footer RAIL + 关闭入口(P1,`quick_look.rs` + `workspace.rs` + `terminal_view/mod.rs`)

- footer 右侧补 `RAIL · n/N` 读数(从活动栏打开时);措辞对齐原型(换文件→改动文件、切 File→Diff)。
- header 右端加显式 ✕ 关闭按钮(hover err-soft),不再只靠 footer Esc。
- 数据通路:新增 `TerminalView::rail_len()` + `QuickLook::set_rail_pos()`,workspace 每帧把 `(ql_rail_idx, 改动文件总数)` 喂入(`None` = 从文件树打开)。

### 状态栏区分会话态(P1,`workspace.rs`)

- sessions 只数**非欢迎** tab:纯新标签欢迎页 → `NO SESSION` + idle 灰点;有实会话 → `N SESSIONS` + 磷光实点。

### 欢迎页 Explorer 决策反转(用户第四轮)

- 改回**常驻 Explorer**,回写 SHEET 07 板 A/A2(canvas 加 240 Explorer 列)。

### 分屏启动器(P1,已核查无需改)

- header h38(L4 float-head)、footer h30、profile 行 h36 且选中 = L4 + 2px 左脊 —— 现有实现已合规,截图为早前态。

### 宠物欢迎页(P2,`design/panels/07-states.html`)

- 查证:品种**随机为产品规则**(`Breed::random`,可 `fixed_breed` 固定;SHEET 05 板 B 已记录)。perch 的 2× / shelf 180 / label 格式实现已对齐。动作 = 在 SHEET 07 板 A note 标注「金毛仅示例,品种每会话随机」,闭合差异条目;不强制金毛。

验证:`cargo fmt` + `cargo build -p tn-ui` 通过;`cargo test -p tn-ui --lib` 167 passed;原型浏览器渲染确认(Explorer 列 + NO SESSION)。

## 第五轮:按二轮像素复审 P0–P2 队列全面修正(差异总结 §10)

### P0-1 主题刷新根因(`tn-config/src/lib.rs` + `theme.rs` + `config/`)

- `write_if_absent` 对 `themes/tn-dark.toml` 改为 **`sync_managed`**:内容与内置版不同即覆盖 —— 旧 Tokyo 调色滞留用户目录(磷光从未到达真机)的根治;`config.toml` 仍 write_if_absent(用户文件不碰)。新增回归测试 `stale_builtin_theme_is_refreshed_on_load`。
- `AgentColors::default()` 旧 Tokyo 值(#F0916D/#73DACA)→ 磷光身份色(claude #C9A8FF / codex #6FB3E8);修正 `ansi_index_order` 测试断言(原断言旧 Tokyo ANSI,与磷光主题文件冲突)。
- `config/config.toml` 示例 agent `accent` #7AA2F7 → #5BE7C4;`tn-dark.toml` 头部加「app 管辖、随版本覆盖」注记。

### P0-2 幽灵终端白 surface(`quick_terminal.rs` + `platform.rs` + `lib.rs`)

- 判定:gpui 0.2 Windows 的 `Transparent` = SetWindowCompositionAttribute accent 渐变(发白「材质」),非逐像素透明 → 弃用透明窗。窗体改 **Opaque**,新增 `platform::set_ghost_region`(圆角矩形上移一个圆径 = 上直角下圆角顶垂形 region),`resnap`/`slide` 重设 region,圆角外像素被裁 → 零白区、隐藏滑出无暴露。
- 残影收编为**卡内刻线**:底缘上方 1px ph-dim 圆角弧(启动器 1 道 / 运行态 2 道,·45/·18 与原型同档);`card_height` 撤掉 16px 窗外残影下沉区。真透明可达后再外移(代码注记)。
- 顶缘召唤线:两段 flex 渐变正中 1px 缺口 → 绝对定位两半 + 中心 2px 负边距重叠;渐变停靠 0.4 起坡(对应原型两端 20% 全透平台)。
- 运行态幽灵头回归(差异 4-3):shell 会话显示 gmark + `GHOST_` + 磷光会话 chip(`pwsh · 标题`)+ 失焦提示 + 磷光点;`TerminalView` 新增 `ghost_chrome` 标记抑制其 shell 板头(agent 头保留 — 用量环不可丢)。
- 启动器默认选中回 pwsh(`default_picker_sel`:第一枚 shell 磁贴,agents 行在前但不抢默认位;差异 4-5)。

### P0-3 QuickLook(`quick_look.rs` + `workspace.rs`)

- scrim 改盖**整个工作区**(含 Explorer,SHEET 03-A stage inset 0;改动文件导航走 ↑↓/RAIL);浮层高度上限 600 → **520**(锁 1080×520)。
- `CODE_BG` L1「凹井」→ **L3**(File/Diff 正文与页脚同海拔;3-5/3-6 海拔断裂修复)。
- 编辑态当前行整行 L4 底 + 行号磷光(3-10,自绘painter新增);块光标 = ui.accent(主题刷新后即磷光)+ 反相墨字(既有)。

### P1/P2 字形身份表 + 部件(多文件)

- 新增 `welcome::agent_glyph_ch`(Claude ✳ / Codex ◆ / 其余 ⟡)+ `launch_glyph_ch` 扩表(wsl ⌬ / pet ⌂ / spark→⟡);`profile_card` 按 agent id 给字形;`wsl_card` 字形 ⌬(WSL 字形矛盾按 HTML 多数定 info 蓝,§9 已定夺);`ssh_card` accent → `ansi.yellow`(warn 琥珀)。
- 命令面板与分屏启动器第二步:`.gi` 无差别色块 → mono 600 12 身份字形(同一映射)。
- tab 字形:`✻` 硬编码 → ✳/◆/⟡ 按 agent id;agent 头像同表;用量环余量轨 白·8% → **L4**(2-6);`short_model` 保留小数点(`GPT 55` → `GPT 5.5`,2-7)。
- QuickLook 头部(3-11~3-15):文件图标 → 磷光 ▪;路径 `/` → ` › ` 面包屑(t3);新增 EXT / 编码·换行 / 行数 三枚 meta chips(预览态);`编辑中`(旧 claude 橙)→ **EDIT 磷光 chip** 紧跟路径 + 「● 未保存」warn chip;「已改动」→ warn;Diff 态新增 **+N/−N** 统计 chips;编辑页脚右端补 **LN n · COL n** 磷光读数(3-18)。

### P1 宠物(差异总结 §8;`pet.rs` + `welcome.rs` + `workspace.rs` + `io.rs`)

- 欢迎页 2× 改为**同一只 PetView 的 on_welcome 形态**(×2 绘制、shelf 180 居中、活标签「品种 · 上下文」):hover/单击/拖拽/右键/气泡全部生效;删除 `sprite_block` 静态贴图与 `pet_breed` 喂入管线;标签与状态栏席位同源(IDLE/RUNNING 撞车不再可能)。
- RUNNING 泄漏根治:`SessionRunGuard`(PTY reader 持有,Drop 时核销未归还的 OutputStart);裸 `signal_command_start/end` 收为私有。
- 双击宠物 = 打开设置菜单(click_count ≥ 2);命令面板新增「宠物设置」行(`LaunchRow::PetSettings`,⌂ t3 + SETTINGS tag,仅根列表;激活 → `PetView::open_settings`)——TODO 待办「宠物设置命令面板入口」就此关闭。

### 验证

- `cargo build -p tn-ui` 通过;`cargo test -p tn-ui --lib` **168 passed**;`cargo test -p tn-config` **43 passed**(含新主题刷新回归);`cargo test --workspace` 全绿;`cargo fmt --all` 已跑。
- 真机回归截图待补(用户操作):重启后旧主题应被覆盖,全套页面重截对照差异总结复核。

## 仍待用户决策的结构性 P0

- Agent/Shell 是否恢复 Explorer + Agent + Shell 多 pane 仪表舱(纯布局,与通用性无关)。

## 本轮未做(差异总结遗留,待后续轮)

- SSH TOFU 信任卡(06-C,未做.md 自报缺失)与未保存确认浮层化(03→06-C 形态);RUN 块条实时耗时;App Menu 分组/注记/kbd 全拼;SSH 子页富列表回填原型 + 页脚键帽统一;分屏两段式定夺;断线重连横幅形态定夺。

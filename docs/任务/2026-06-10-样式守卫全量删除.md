# 样式守卫全量删除

## 定位

本文记录“将样式守卫全删,后续样式由项目自行决定”的执行过程。

## 背景

旧样式体系包含 `token_drift`、`spec_gen` 和硬编码主题色扫描等测试守卫,会把实现约束到 `design/mockup.html`、`design/calm-glass.css` 和旧的 `docs/界面样式/` 文档。项目文档已清理并准备重新设计,这些守卫不再符合当前方向。

## 具体内容

- [x] 删除 `crates/tn-ui/src/style.rs` 中样式 token 漂移、spec 生成和硬编码主题色扫描测试。
- [x] 清理代码、设计原型和注释中指向旧样式守卫/旧样式文档的引用。
- [x] 运行 targeted Rust 测试和文档校验。
- [x] 在本文记录验证结果后,将 `TODO.md` 本任务移动到已完成。

## 验证 / 状态

- 状态:已完成。
- `cargo test -p tn-ui --lib`:通过,173 个测试通过。
- `git diff --check -- TODO.md docs crates design config`:通过。
- 样式守卫关键词扫描:源码和设计文件中不再存在 `token_drift`、`spec_gen`、`TN_GEN_SPEC`、`no_hardcoded_theme_colors` 等守卫入口。

## 反向链接

- [TODO](../../TODO.md)
- [文档治理索引](../文档治理索引.md)

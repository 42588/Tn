# 大型文档拆分为母文档索引 + 子文档详情

## 状态

进行中。

## 目标

把项目内所有长期维护文档逐步改造成“母文档精准定位子文档、子文档展示具体信息”的一对多结构,降低单文件膨胀和长期记忆漂移。

## 当前记录

本任务从现在开始用 `TODO.md` 跟踪。`TODO.md` 只保留总任务状态和下一步;本文负责记录全量范围、阶段清单、验证结果和未完成事项。

## 2026-06-09 子智能体分工记录

- `AGENTS.md` 子任务:把 Codex 入口改为同级短索引,并创建共享协作规则落点。
- `CLAUDE.md` 子任务:把工作区构建、后续事项、踩坑记录迁入 `docs/共享记忆/`。
- `CHANGELOG.md` 子任务:把发布摘要保留在根文件,把远端文件服务、智能体宿主、应用内智能体编辑器和快速预览编辑器细节迁入专题子文档。
- 主控整合:统一新增文件中文命名、补齐共享记忆链接、把新专题反向纳入母索引,并执行链接与格式验证。

## 2026-06-09 docs 剩余文件重构记录

- 样式域:把 `docs/界面样式实现规则.md` 收束为母索引,新增 `docs/界面样式/` 下的设计真源、翻译规则、视觉守卫和组件规格子文档。
- 修复与优化域:把 `docs/修复与优化记录索引.md` 收束为日期/主题索引,新增或整理基础性能、终端滚动、快速预览、智能体活动栏、文件树和远端连接专题。
- 已知问题域:把 `docs/已知问题索引.md` 收束为问题索引,将快速预览、远端文件系统、输入键位、真机视觉、欢迎页数据源和待澄清样式问题拆入子文档。
- 架构域:把 `docs/系统架构索引.md` 收束为系统边界和子系统入口,新增 `docs/架构/` 下的总览、代码包边界、终端数据流、智能体宿主、远端安全外壳、编辑器和路线图专题。
- 新增模块域:把 `docs/新增模块索引.md` 收束为模块索引,将安全外壳远程连接与连接体验拆入中文命名子文档。
- 产品与参考域:把 `docs/产品体验索引.md` 和 `docs/外部参考资料索引.md` 收束为母索引,新增产品体验与参考资料专题目录。
- 主控整合:修正跨目录相对链接、旧章节号引用和英文缩写文件名残留,保持 `CLAUDE.md` 与 `AGENTS.md` 同级入口关系。

## 文档盘点范围

- 根入口: `AGENTS.md`, `CLAUDE.md`, `TODO.md`。
- 项目介绍与变更: `README.md`, `CHANGELOG.md`;`README.zh-CN.md` 在当前工作区未发现,若后续补回再纳入。
- docs 目录: `docs/系统架构索引.md`, `docs/产品体验索引.md`, `docs/界面样式实现规则.md`, `docs/修复与优化记录索引.md`, `docs/新增模块索引.md`, `docs/已知问题索引.md`, `docs/外部参考资料索引.md`。
- 后续新增子文档目录: `docs/任务/`、`docs/架构/`、`docs/产品体验/`、`docs/界面样式/`、`docs/修复与优化/`、`docs/已知问题/`、`docs/新增模块/`、`docs/参考资料/`、`docs/共享记忆/` 已建立。
- 文档治理方案: `docs/文档治理/文档盘点与首批拆分方案.md` 已建立,负责记录盘点表、首批目录命名、母文档模板和拆分顺序。

## 初始拆分对象

- `CLAUDE.md` / `AGENTS.md`: 作为同级 agent 入口,只保留各自索引和必要差异;共享规则沉到 `docs/` 权威文档。
- `docs/修复与优化记录索引.md`: 保留日期和主题索引,把具体优化记录拆到 `docs/修复与优化/` 下的主题文件。
- `docs/已知问题索引.md`: 保留问题索引和优先级,把具体根因、复现、限制拆到 `docs/已知问题/` 下的专题文件。
- `docs/系统架构索引.md`: 保留架构导航,把 crate / 子系统细节拆到架构子文档。
- `docs/界面样式实现规则.md`: 保留样式入口和守卫规则,把组件级细节拆到样式子文档。
- `docs/新增模块索引.md`: 保留模块索引,把模块详情拆到功能/子系统文档。
- `CHANGELOG.md`: 保持发布向变更记录;不承载详细设计,只链接到对应任务或专题文档。

## 拆分原则

- 按主题拆,不按行数拆。
- 母文档只放摘要、状态、日期和链接。
- 子文档是具体信息的权威落点。
- 同一事实不在多个母文档重复铺开。
- 迁移时先保持语义不变,再做必要的标题和索引整理。

## 验收标准

- 每个被拆的大型母文档都能快速定位到对应子文档。
- 子文档标题能清楚说明自己负责的主题。
- 原有有效信息没有丢失。
- 新增信息规则已写入 `AGENTS.md` 和 `CLAUDE.md`。
- `TODO.md` 能准确显示当前阶段、下一步和任务详情入口。

## 验证记录

- 2026-06-09 docs 剩余文件重构后,旧文件名、旧章节号和旧专题名扫描无命中。
- Markdown 相对链接检查已覆盖 `AGENTS.md`、`CLAUDE.md`、`CHANGELOG.md`、`TODO.md`、`README.md` 和 `docs/**/*.md`;仅剩 `README.md` 的既有 `../../releases` GitHub Releases 路径被本地检查器报为非本地文件,本轮未改动。
- `git diff --check -- docs TODO.md` 通过。
- 2026-06-09 严格审查修复:样式规格生成器目标改为 `docs/界面样式/组件规格与自动生成数值.md`,清理旧母文档章节号、过期 SSH parked 状态、治理规则和新增模块反向链接。
- `cargo test -p tn-ui spec_gen` 通过。
- Markdown 相对链接检查已清零。
- `cargo test -p tn-ui --lib` 仍有 1 个既有 Quick Look WIP 测试失败:`quick_look::tests::diff_jump_target_carries_file_highlight_row`,本轮未改动其行为逻辑。

## 阶段清单

- [x] 建立 `TODO.md` 母索引和 `docs/任务/` 任务详情目录。
- [x] 在 `AGENTS.md` / `CLAUDE.md` 中写入新增信息落点规则。
- [x] 将本文档改造任务切换为进行中。
- [x] 将 docs 根目录原有母文档更名为职责清晰的中文文件名,并更新全仓引用。
- [x] 盘点所有文档长度、主题密度和引用关系。
- [x] 设计首批子文档目录命名和母文档索引格式。
- [x] 先拆 `CLAUDE.md` 的工作记忆/踩坑类内容。
- [x] 重构 `AGENTS.md` 为 Codex 同级入口,并沉淀共享协作规则。
- [x] 重构 `CHANGELOG.md` 为发布摘要,把具体实现细节链接到专题子文档。
- [x] 继续拆 `docs/修复与优化记录索引.md` 与 `docs/已知问题索引.md`。
- [x] 拆 `docs/系统架构索引.md`、`docs/界面样式实现规则.md`、`docs/新增模块索引.md`。
- [x] 拆 `docs/产品体验索引.md` 与 `docs/外部参考资料索引.md`。
- [ ] 检查 `README.md` 是否只需链接化调整;若恢复 `README.zh-CN.md`,同步纳入。
- [ ] 全量检查链接、重复信息和孤儿子文档。

## 后续

下一步检查 `README.md` 是否需要链接化调整,并做全量重复信息、孤儿子文档和跨域链接复核。

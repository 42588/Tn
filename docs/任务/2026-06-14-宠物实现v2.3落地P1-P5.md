# 宠物实现 v2.3 落地(P1–P5)

> 2026-06-14。按 `docs/宠物/宠物系统规则.md`(v2.3)与 SHEET 05 图纸,把宠物从
> v1 基座推进到 v2.3 全量。原型(`design/panels/05-pet-system.html`)是设计真源,
> 实现落在 `crates/tn-ui/src/pet.rs`(共情信号经 `terminal_view/io.rs`,磷光通道
> 接线在 `workspace.rs`)。按规则「实现分期」分五次提交,P1 无需审核稿,P2–P5 的
> 新帧/新动效由用户一次性审核通过。

## 提交序列

| 期 | 提交 | 规则 | 内容 |
|---|---|---|---|
| P1 | `8a41d71` | C/D | 性格参数表 + 活物引擎 |
| P2 | `57d3163` | 0/A/E/F | 领养命名 + 每日见面 + 羁绊档案 + 工作共情 |
| P3 | `debf438` | B | 小饼干投喂 |
| P4 | `07696dc` | G/H/I | 主动互动 + 互动记忆 + 记忆透明/重置 |
| P5 | `ab008f9` | J | 磷光通道 `PetSpatialCue`(初版) |
| P5+ | `56d30a4` | J | 磷光通道改版:全屏光点飞向 UI 接缝(修「头顶一根线」) |

## P1 · 性格 + 活物引擎(规则 C/D)

- `Personality` 七犬常量表(表即实现):眨眼区间 / Idle 尾摆幅度 / 打盹阈值 /
  口癖 / 微动作权重 / 入场权重;替换原硬编码。德牧无口癖 → 单击/Play 点头代替。
- `Micro` 六动作活物引擎(抓痒 / 舔爪 / 追尾水平镜像 / 竖耳 / 望屏外 / 伸懒腰):
  纯 Idle 下 20–60s 随机加权触发、≤1.5s、不连续两次、≤2 次/分钟、离 Idle 弃帧、
  reduced motion 停用;全部复用既有 dx/dy/镜像,**零新网格**。

## P2 · 领养命名 / 每日见面 / 档案 / 共情(规则 0/A/E/F)

- **规则 0 身份**:首次启用 → 一次性领养卡(品种架 + 交给缘分 + 名字字段,
  ≤8 字、默认品种名、Esc 跳过);改名浮层;换个形象品种架;**随机刷新废除**。
  名字三处上屏(状态栏席位 / 档案行 / 欢迎页 2× 标签)。文本输入沿用命令面板
  同款 `track_focus + on_key_down` 累积(CJK 走 key_char)。
- **规则 A 每日见面**:本地自然日首现 → 入场四式(探头 / 小跑 / 天降 / 它先到了,
  性格加权、不重样)+ 时段问候(清晨/白天/傍晚/深夜)+ 第N天短显 5s;深夜问候
  补哈欠帧。本地日期/小时用 Win32 `GetLocalTime`(新增 Cargo 特性
  `Win32_System_SystemInformation`)。
- **规则 F 羁绊档案**:`pet_state.json` 增 first_met / days_together /
  last_seen_date / treats_fed;里程碑词 7/30/100/365 天。无奖励、无解锁。
- **规则 E 工作共情**:结构化事件派生(exit code + 运行时长 + `git commit/push`
  首词),零输出扫描。三连胜 / 大功告成(>60s)/ 翻盘 / 提交时刻装点 Success 演出
  (双 / 单爱心);≥3 连败 → 就近趴下陪着 +「呜…」一次(永不评判);共享冷却
  ≤2 次/10 分钟。信号经 `SessionRunGuard` + `io.rs` 的 `CommandLine` 接入。
- 把变化检测元组改为 `frame_fingerprint`(u64),绕过元组 12 元 PartialEq 上限。

## P3 · 小饼干(规则 B)

- 菜单首组「给小饼干 · 今日还有 1 块」→ 喂过置灰「今天吃过啦」;每自然日 1 块
  (`last_treat_date`),不囤积、不补偿、不提示;新的一天自动补货(每日见面)。
- 新增 `Feed` 上下文 + 四帧演出:饼干(4×4 #C99052 非磷光)抛物线入场 → 三帧律
  接住 → 双腮交替咀嚼 → 单爱心 + 快摆尾。深夜彩蛋:吃完哈欠 → 主动趴下。

## P4 · 主动互动 / 互动记忆 / 透明(规则 G/H/I)

- **规则 G**:摸头(长按 350ms 不移动 → 头低眯眼 + 爱心;拖拽改为需真正位移)·
  叫名字(菜单,10s 冷却)· 换窝安顿(松手记常用窝象限 + 落地回弹嗅地)·
  摆个玩具(球/骨头/小毯子/收起,岗台暖色非磷光)· 久别亲近(≥3 日 → 多一颗心,
  无愧疚文案)。
- **规则 H 互动记忆**:`pet_state` 增 fav_pat/feed/play/call(衰减计数)、
  favorite_perch(象限)、toy、quiet_hours_seen、last_affection_at;只记偏好不做画像
  (recency-weighted、固定上限)。
- **规则 I 透明/清除**:菜单显示「它记得:≤3 标签」/「还在认识你」;
  「重置互动记忆…」二次确认卡只清五项偏好,不动名字/品种/档案/窝坐标/开关。

## P5 · 磷光通道(规则 J)

- **`PetSpatialCue`**:UI 控制层显式发出的一次性转场 cue(只含转场语义,不读终端
  内容)。宠物在岗台接缝点亮**磷光锯齿裂缝 + 光点**:开门类裂缝亮起变长 + 蓄力下蹲,
  召回类收束熄灭,切换类只闪光点。颜色只用 ph 体系(愈合端用实心暗磷光,因 PH_DIM
  含 alpha 不能直接 rgb),绝不外发光。
- **节律(规则 J4)**:同一时刻只一个 cue(新替旧);≥30s 完整共演,否则短版
  ≤350ms;**直接互动(拖拽/摸头/投喂/逗弄)优先**,期间通道静默不抢戏;reduced
  motion 只 1px 磷光点闪一下。30ms 自停驱动;折进 `frame_fingerprint`。
  **UI 本体绝不等待宠物动画**(调用方发完即继续)。
- **已接线发射点(`workspace.rs`)**:`OverlayOpen`(命令面板)·
  `QuickLookOpen`/`Close`/`Switch`(资源管理器点开 + 键盘开关 + ↑↓ 换文件)·
  `SplitCreate`(分屏)· `WelcomeToWorkspace`(欢迎页启动)· `RemoteReconnect`(SSH 重连)。
- **未接线**:`GhostEnter`/`GhostExit` —— 幽灵终端是独立顶层窗口
  (`quick_terminal.rs`),与主窗宠物 overlay 不同窗,标注 `#[allow(dead_code)]`,
  作为跨窗方案的远期跟进(板 H)。

### P5+ 改版(`56d30a4`):全屏光点飞向 UI 接缝

初版只在宠物 100×84 盒内画 3 格裂缝,够不到屏幕中央的面板 → 用户实测「只是头顶
一根细线,没有打开窗口的感觉」。改版把演出搬到全屏:

- **`channel_overlay()`**:全屏 canvas。一颗磷光光点从**宠物头顶飞过整个屏幕**到达
  该转场的真实 UI 锚点(命令面板顶缘 / Quick Look **左侧边** / 分屏中线 / 欢迎区 /
  远端 pane / 屏顶召唤线),沿弧线行进 + 5 点渐隐残影拖尾,到达后在接缝**绽放磷光
  锯齿裂缝 + 亮点**(开门到达即绽,召回离开前绽,切换只脉冲)。这才是「宠物打开空间」。
- **层级修复**:宠物 overlay 提升为 workspace **最顶层**子节点,光点/裂缝才能盖过
  Quick Look / 命令面板的 scrim(此前在 scrim 之下被压暗到 ~38%);宠物根仍穿透,
  cue 驱动 notify 重绘自身,动画不被冻结。
- **时长**:完整 1100ms、短版 640ms(旧 320ms 跨屏太快看不清);reduced motion 锚点
  1px 闪。
- **遗留**:锚点为视口近似(非精确控件几何);宠物本体不再随浮层 scrim 变暗(可接受,
  规则只要求暂停主动演出,非压暗)。

## 验证

- `cargo test -p tn-ui --lib pet` → 10 绿(性格表 / 垂耳 / 入场权重 / 时段 /
  里程碑 / 提交判定 / 久别日期差 / 窝象限标签 + 既有 2 个网格守卫)。
- `cargo test -p tn-ui --lib` → 全绿;`cargo build -p tn-app` 成功。
- `cargo clippy -p tn-ui` 未引入新告警(pet.rs / workspace.rs 余留均为既有:
  相位翻转同块、菜单闭包复杂类型、agent_dir_picker 等)。
- **待真机**:七犬手感、入场四式与时段问候、投喂四帧、摸头/叫名/换窝/玩具、记忆
  透明行与重置、连败陪伴趴下,以及磷光通道在命令面板/Quick Look/分屏/欢迎页/SSH
  重连五处的裂缝+光点演出节律。

## 远期跟进

- `GhostEnter`/`GhostExit` 跨窗接线(幽灵终端独立窗口)。
- 串门偶遇(行走帧)、节日皮肤、岗台季节小物(各自单独审核稿)。

## 反向链接

- [宠物系统规则](../宠物/宠物系统规则.md)
- [SHEET 05 宠物系统图纸](../../design/panels/05-pet-system.html)
- [宠物图纸 v2.3 重设计](2026-06-13-宠物图纸v2.3重设计.md)

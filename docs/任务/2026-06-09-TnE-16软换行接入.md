# TnE-16 软换行接入

## 状态

已完成。

## 目标

把 `tn-editor::LineLayout` 的软换行模型接入 Quick Look 的 File / Edit 渲染与命中路径:Markdown、txt、log、说明文档默认软换行,代码文件继续横向滚动。

## 范围

- 只认领 TnE-16,不合并 TnE-18/19/20/21。
- 不改 Diff accept/reject、不改变远端路径命名空间。
- 在已有 `quick_look.rs` 改动基础上继续,不回滚当前未提交的 TnE-13 / agent surface 相关差异。

## 执行清单

- [x] 读取 TnE 执行包、架构、产品体验和待验证文档。
- [x] 确认当前工作区已有未提交改动,决定就地工作并避免覆盖。
- [x] TDD:新增软换行接入测试并确认先失败。
- [x] 实现 File / Edit 软换行接入。
- [x] 运行 `cargo test -p tn-editor` 与 `cargo test -p tn-ui --lib`。
- [x] 同步已知问题、执行包状态和本任务验证结果。

## 验证

- RED: `cargo test -p tn-ui --lib markdown_file_uses_visual_soft_wrap_while_code_keeps_horizontal_scroll` 先因缺少 `file_wrap_mode_for_path` / `quicklook_file_layout` 失败。
- GREEN: `cargo test -p tn-ui --lib markdown_file_uses_visual_soft_wrap_while_code_keeps_horizontal_scroll` 通过。
- `cargo test -p tn-ui --lib`: 156 passed, 0 failed。
- `cargo test -p tn-editor`: 27 passed, 0 failed; doc-tests 0 passed, 0 failed。

未做真机 GUI 复验:仍建议 owner 打开长 Markdown/txt/log,覆盖跨视觉行滚动、选区、复制、查找跳转;代码文件覆盖横向滚动。

## 后续

下一包是 TnE-18 typing effects;不得在本任务顺手实现。

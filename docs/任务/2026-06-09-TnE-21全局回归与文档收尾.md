# TnE-21 全局回归与文档收尾

## 定位

记录 Quick Look / Tn Editor 收尾包 TnE-21 的自动回归、状态清理、文档同步和剩余真机验证债。

## 背景

TnE-16 到 TnE-20 已连续完成软换行、IME / selection 修复、typing effects、Quick Look 外部变更处理、Editor Pane 升级和未保存关闭提示。TnE-21 不新增功能,只把完成状态、验证结果和仍需真机复验的项目收束到正确源文档。

## 具体内容

- [x] 读取 `Quick Look / Tn Editor` 执行包、用户体验升级、待验证清单、架构、产品体验和近期变更记录。
- [x] 运行 `cargo test --workspace --lib`。
- [x] 运行 `cargo build --workspace`。
- [x] 运行 Markdown 链接 / 空白检查。
- [x] 运行 `$env:TN_AUTOQUIT="1"; cargo run -p tn-app`。
- [x] 更新 TnE-21 状态、已知问题索引、产品体验索引、系统架构索引和近期变更记录。

## 验证 / 状态

- `cargo test --workspace --lib` 通过:tn-agent 14、tn-ai 18、tn-blocks 5、tn-config 42、tn-core 33、tn-editor 27、tn-pty 28、tn-shell 13、tn-ui 177,全部 0 failed。
- `cargo build --workspace` 通过。
- `git diff --check -- AGENTS.md CLAUDE.md TODO.md docs` 通过。
- Markdown 相对 `.md` 链接检查通过:77 个文档文件无缺失目标。
- `$env:TN_AUTOQUIT="1"; cargo run -p tn-app` 通过并输出 `TN_GUI_OK` 网格;运行日志包含 DXGI debug interface 缺失 warning,退出码为 0。
- 真机验证债不在本包内伪装完成;仍按 [快速预览编辑器待验证与退出条件](../已知问题/快速预览编辑器待验证与退出条件.md) 保留。

## 反向链接

- 当前队列:[TODO](../../TODO.md)
- 执行包:[快速预览编辑器执行包指导](../已知问题/快速预览编辑器执行包指导.md)
- 待验证:[快速预览编辑器待验证与退出条件](../已知问题/快速预览编辑器待验证与退出条件.md)
- 架构:[编辑器与快速预览](../架构/编辑器与快速预览.md)
- 产品体验:[快速预览与编辑](../产品体验/快速预览与编辑.md)

# 文档管理方案 skill 化

## 状态

已完成。

## 目标

把当前文档管理方案沉淀为可自动发现的个人 Codex skill,让后续 agent 在维护 Tn 文档时能稳定遵守 `TODO.md`、`docs/任务/`、母索引、子文档、共享记忆、已知问题和验证状态的分工。

## 范围

- 创建个人 skill: `C:\Users\Gua\.codex\skills\managing-project-docs`。
- 编写 `SKILL.md`,覆盖任务登记、信息落点、母索引契约、子文档契约、更新顺序和常见错误。
- 编写 `agents/openai.yaml`,提供 UI 名称、短描述和默认提示。
- 补充项目初始化流程:检测必需文档标记,缺失时自动创建最小中文文档体系,并按规则改造 `AGENTS.md` / `AGENT.md` / `CLAUDE.md`。
- 从用户体验角度优化 skill:加入少问、少读、少写、少打扰、少 token 的原则,并将初始化流程改为先探测、再按需补齐、最后轻量验证。
- 整理 GitHub 发布包到 `github/managing-project-docs-skill`,移除本机路径和项目专名,保留通用中文文档治理能力。
- 用现有文档漏洞修复记录作为 baseline failure 来源,确保 skill 明确覆盖任务看板不承接活跃事项、状态词混用、实现/验证状态不分栏和只读审查转修复时未登记等问题。

## 执行清单

- [x] 阅读 `TODO.md`、文档治理索引、文档盘点与首批拆分方案、共享记忆入口和相关任务记录。
- [x] 用 `skill-creator` 初始化 `managing-project-docs` skill 骨架。
- [x] 将模板替换为最小可用的文档管理流程说明。
- [x] 补齐 `agents/openai.yaml`。
- [x] 扩展触发描述,覆盖项目初始化、仓库接入、缺失 `TODO.md` / `docs/文档治理索引.md` / 入口文件时的场景。
- [x] 增加 Project Initialization 流程和 Bootstrap Artifact Set。
- [x] 增加 `AGENTS.md` / `AGENT.md` / `CLAUDE.md` 入口文件改造规则。
- [x] 增加 UX Principles,明确少问、少读、少写、少打扰、少 token。
- [x] 增加 Low-Token Workflow,规定 Probe / Patch / Verify 三段式流程。
- [x] 将 bootstrap 清单拆为 required artifacts 与 optional domain indexes,避免创建空架子。
- [x] 创建 GitHub 发布包目录 `github/managing-project-docs-skill`。
- [x] 泛化发布版 `SKILL.md`,去掉 `Tn` 专名和本机路径。
- [x] 补充发布版 `README.md`、安装说明和验证建议。
- [x] 运行官方校验脚本并记录环境阻塞。
- [x] 用不依赖 PyYAML 的静态检查覆盖 frontmatter、命名、描述长度、TODO 残留和 UI 元数据。

## 验证

- 官方 `quick_validate.py` 未能完成:当前 Python 环境缺少 `yaml` / PyYAML,脚本在 import 阶段退出。
- 手动结构检查通过:
  - `SKILL.md` frontmatter 存在。
  - `name` 为 `managing-project-docs`,符合小写 hyphen-case,长度小于 64。
  - `description` 以 `Use when` 开头,长度 238,不含尖括号。
  - `SKILL.md` 不含模板 TODO 残留。
  - `agents/openai.yaml` 存在。
  - `short_description` 长度 30,符合 25-64 字符限制。
  - `default_prompt` 包含 `$managing-project-docs`。
- 初始化增强后追加检查通过:
  - `description` 仍以 `Use when` 开头,长度小于 1024。
  - `SKILL.md` 包含 `## Project Initialization`。
  - `SKILL.md` 覆盖 `Bootstrap Artifact Set` 对应的必需文档标记。
  - `SKILL.md` 覆盖 `AGENTS.md` / `AGENT.md` / `CLAUDE.md` 入口文件改造规则。
  - `agents/openai.yaml` 默认提示已同步为初始化或维护场景。
- 用户体验优化后追加检查通过:
  - `SKILL.md` 包含 `## UX Principles`。
  - `SKILL.md` 包含 `## Low-Token Workflow`。
  - `SKILL.md` 明确 required bootstrap artifacts 与 optional domain indexes。
  - `SKILL.md` 明确不要全量读取 `docs/` 或创建所有可选索引。
  - `agents/openai.yaml` 默认提示已同步为 smooth low-token workflow。
- GitHub 发布包检查通过:
  - 发布目录包含 `README.md`、`managing-project-docs/SKILL.md`、`managing-project-docs/agents/openai.yaml`。
  - 发布版 `SKILL.md` frontmatter、名称、描述长度、UI 短描述和默认提示检查通过。
  - 发布版未包含本机路径、项目任务记录或模板 TODO 残留。
  - `git diff --check -- github/managing-project-docs-skill` 通过。

## 后续

- 若后续安装 PyYAML 或官方脚本改为无依赖读取,可重跑:

```powershell
python -X utf8 C:\Users\Gua\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\Gua\.codex\skills\managing-project-docs
```

## 反向链接

- 任务母索引: [TODO.md](../../TODO.md)
- 治理规则: [文档盘点与首批拆分方案](../文档治理/文档盘点与首批拆分方案.md)
- 相关任务: [文档功能逻辑漏洞修复](2026-06-09-文档功能逻辑漏洞修复.md)

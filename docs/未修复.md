# 已知限制 / 待修(非路线图项)

> 这里只记**当前真实存在、但够不上里程碑级别**的小限制 / 待办 / parked。
>
> - **里程碑级路线图** → [架构蓝图 §8](架构蓝图.md)(含 M2 SSH parked、自定义 `TerminalElement`、拖拽停靠、配色导入等)。
> - **踩过的坑(已修 + 教训)** → [CLAUDE.md](../CLAUDE.md)「踩过的坑」。
> - **逐项优化明细** → [优化日志.md](优化日志.md) / [CHANGELOG.md](../CHANGELOG.md)。
>
> 修掉一项就从这里删,别让它变回一潭旧账。

## Quick Look 速览 / 编辑器

- **散文 / Markdown 长段落不能软换行**:固定行高 `uniform_list`(只 measure row 0)→ wrap 会变高破坏虚拟化,长段落只能横向滚。prose 体验欠佳。要 wrap 得放弃该场景虚拟化或改测每行高。
- **Diff tab 不支持拖选**:鼠标拖选只做了 File / 编辑态;Diff 行未接。
- **编辑态每键 `Rc::make_mut` 深拷整 buffer**:412 行亚毫秒、非冻结主因,但接近 `MAX_LINES`(4000)会有延迟感;真要治改 `Rc<RefCell<Vec<String>>>`。

## 输入 / 键位

- **命令面板中文搜索 parked**:面板靠 `on_key` 读 ASCII,IME 合成的中文只经 `EntityInputHandler` 投递、面板未接 → 目前只能搜英文。补法见 CLAUDE.md「未做/后续」。
- **`Ctrl+Shift+*` 可能被中文 / 多布局 Windows 的 IME / 系统热键吞**:真机不达时可改键,或把系统「输入语言热键」设未分配。
- **IME 合成键子类化修复(`VK_PROCESSKEY`)真机待验**:理论成立(IMM32 标准),须真机肉眼 + 看日志(`RUST_LOG=tn::ime=debug`)确认微软拼音确以 `VK_PROCESSKEY` 投递合成键。

## 真机肉眼待验(headless 验不了)

- 颜值微调 · 标题栏拖动/控制点 · Quick Terminal 动画顺滑 / 多显示器 / 高 DPI 定位 / 首帧不空白(M5 真机验待 owner)。
- per-pane cwd 经 OSC 7 实时跟随。

## 待接数据源

- **欢迎页「最近目录」**:需 recent-sessions 数据源(agent 会话 cwd + mtime)。

## 远端 / WSL 文件系统

- **SSH / macOS 远端文件树仍待接**:当前已把 SSH/macOS/Linux 远端 cwd 从 Windows `PathBuf` / Explorer / git watcher 隔离,焦点切到远端 pane 时不重定左侧 Explorer root,不会再把 `/home/...` 或 `/Users/...` 当作 Windows 目录;但还没有 SFTP/远端文件服务,所以远端文件浏览和 Quick Look 仍只能等后续远端 FS 后端。
- **WSL 已走独立 Explorer root**:WSL 启动不继承 Windows cwd,文件树跟随时由 `ExplorerRoot::Wsl` 显式映射到 `\\wsl$\<distro>\...` 并驱动 Linux 目录树,而不是 `C:\`/`D:\`。

## Tn Editor 用户体验升级:从 Quick Look 快编到可扩展编辑器

当前 Quick Look 编辑器已经能完成“点开文件、快速修改、保存”的闭环,但它仍然是一个塞在 `quick_look.rs` 里的轻量快编器。继续直接堆软换行、多光标、Diff 拖选、LSP、打字动画,会让用户体验变得不可预测:有些场景很漂亮,但保存不安心、长文件不流畅、远端文件不可用、动画还可能拖慢输入。

这项优化的目标不是把 Tn 变成 VS Code,而是把编辑能力升级成 **Tn 工作流里的可信编辑器**:打开快、编辑稳、保存安全、审查 agent 改动顺手,动画作为细节增强而不是主目标。

**用户视角目标**

- **不离开上下文**:用户在终端、文件树、agent 改动卡片之间流转时,能直接预览、编辑、保存,不需要跳到外部编辑器。
- **不怕误改和丢数据**:编辑前后有 dirty 状态、撤销/重做可靠、保存前能发现磁盘外部变化,关闭浮层/切文件时不会静默丢修改。
- **读得舒服**:代码长行继续横滚,Markdown/日志/说明文档应支持软换行;大文件要能快速只读打开,不能因为一个文件拖垮 UI。
- **改得顺手**:鼠标选区、键盘选择、查找替换、复制粘贴、Diff 查看和快速保存都要统一;未来多光标/列选/LSP 要有清晰落点。
- **反馈高级但不打扰**:打字动画、caret 过渡和插入字符反馈应该让输入更有质感,但任何时候都不能牺牲“光标真实位置立即到位”和“连续输入不卡”。

**产品形态**

- **Quick Look 快编**:仍是默认入口。文件树 `Space`/`Enter`、agent 改动卡片、设置文件都先打开浮层。适合看文件、小改动、配置修改、快速 Diff。
- **Editor Pane**:当用户需要长时间编辑或多文件处理时,Quick Look 可升级为正式编辑 pane/tab。它共享同一编辑核心,但拥有更稳定的空间、状态栏和后续 LSP 能力。
- **Diff Review**:agent 改动卡片进入审查视图。短期支持 Diff 拖选/复制/跳转;中期支持 hunk 级 accept/reject、打开对应文件定位编辑。

**架构方向**

- 新增 headless `tn-editor` 层,把编辑核心从 `quick_look.rs` 中拆出。它不依赖 GPUI,负责 `Document`、`CursorSet`、`Selection`、`EditTransaction`、`UndoStack`、`SearchState`、`LineLayout`、编码/换行符、dirty 状态和保存冲突检测。
- UI 层新增 `editor::element` / `QuickLookCodeElement`,用 GPUI `Element` 的 `layout/prepaint/paint` 统一绘制文本、行号、选区、搜索命中、diff decoration、IME preedit、caret 和 typing effects。
- Quick Look、Editor Pane、Diff Review 都消费同一 `tn-editor` 文档模型和同一 renderer。Quick Look 只是宿主形态之一,不再拥有完整编辑业务。
- 文件访问继续受命名空间约束:Host 可直接读写;WSL 走 `\\wsl$` 映射;SSH/macOS 远端必须等 SFTP/远端 FS 后端,不能把远端路径伪装成本机 `PathBuf`。

**编辑体验要求**

- **保存安全**:打开文件时记录 mtime/size/hash;保存前若磁盘已变化,提示用户选择覆盖、重新加载、另存为或查看差异。默认不做静默覆盖。
- **关闭安全**:Quick Look 关闭、切文件、切 Diff、退出应用时,若当前文档 dirty,必须给出保存/放弃/取消。小改动也不能静默丢。
- **换行与编码**:保留原文件换行符风格和编码信息。无法可靠识别时明确降级为 UTF-8 文本编辑或只读预览。
- **大文件策略**:小文件可编辑;接近阈值时提示性能风险;超大文件默认只读流式/分页预览。用户需要强制编辑时必须明确确认。
- **软换行策略**:代码默认横滚;Markdown、txt、log、说明文档默认软换行。实现上采用 logical line -> visual line 映射,视觉行仍固定行高,避免破坏虚拟化。

**打字动画纳入范围**

打字动画保留,但它是 renderer 的可选 effect,不是编辑核心能力。

- 逻辑 cursor、selection、undo、文本内容必须在输入事件提交时立即更新;动画只能表现视觉过渡。
- 允许的效果:同一视觉行内 60-90ms caret 轻微 glide,块光标 squash/pop,新插入字符 70-110ms 的轻微亮度/alpha settle。
- 禁止的效果:不允许在 `uniform_list + div/span` 外层用绝对叠层和 16ms 帧泵推动光标;不允许积累 per-char 动画队列;不允许让动画改变文本宽度、行高或真实命中测试。
- 自动 snap 场景:IME composition、拖选、选区存在、查找跳转、PageUp/Down、跨行移动、滚动、文件切换、窗口失焦、大文件、高帧耗时。
- 配置项:提供 `editor.animations = "off" | "subtle" | "full"` 或 reduced-motion 开关。默认建议 `subtle`;性能不达标时自动降级到当前瞬时反相块。

**渲染方案**

- `EditorElement::prepaint` 计算可见 visual lines、行号槽、文本区、caret rect、选区 rect、IME candidate bounds 和 scrollbar geometry。
- `EditorElement::paint` 只绘制可见区域。文本 shaping 缓存按 `(line_id, line_revision, theme_revision, font_key, wrap_mode)` 失效。
- 固定单元格模型保留:ASCII 1 列,CJK/宽字符 2 列。软换行在 visual line 层做,不改变字符命中模型。
- Diff 视图作为 decoration 渲染,不单独维护另一套文本控件。File / Edit / Diff 三态共用选择、复制、滚动和命中测试。

**分阶段路线**

1. **核心抽离**:建立 `tn-editor`,迁移当前 `op_*`、选区、查找替换、undo/redo、保存逻辑。Quick Look 行为保持不变,先解决 `Rc::make_mut` 深拷和保存冲突基础设施。
2. **只读 Renderer Spike**:在 File 预览态接入 `EditorElement` 只读路径,验证文本绘制、行号、纵横滚、CJK 命中、预览拖选和复制。
3. **编辑态接入**:复用 `tn-editor` transaction,接入单光标、选区、IME、查找替换、保存、dirty 状态。先使用瞬时反相块,不启用动画。
4. **Diff Review 接入**:Diff tab 复用同一 renderer,补拖选/复制/hunk 跳转;后续再做 accept/reject hunk。
5. **软换行**:实现 logical line -> visual line 表。先对 Markdown/txt/log 开启,代码保持横滚;确认虚拟化和鼠标命中稳定后再开放配置。
6. **打字动画**:在 renderer effect 层加入 subtle 动画,默认可关、可降级。用 `TN_PERF` 记录连续输入、中文长行、4000 行文件和 IME 场景。
7. **Editor Pane**:Quick Look 增加“打开为编辑器”入口,共享 `tn-editor` 文档会话。再考虑多文件标签、LSP、diagnostics、补全。

**通过标准**

- 打开普通文本文件即时可预览;进入编辑不改变原有快捷键心智。
- 连续输入 200 字符无明显卡顿,真实 cursor 不落后文本状态。
- 关闭动画 / reduced-motion / 高负载自动降级时,体验回到当前瞬时反相块且无行为差异。
- 中文长行点击、拖选、横滚、IME candidate 位置与当前版本同等精确或更好。
- Markdown/txt/log 软换行不会破坏滚动、选区、复制和查找。
- 4000 行文本滚动和编辑可用,无整面板每帧重建导致的 CPU 尖峰。
- 保存冲突、dirty close、外部修改检测有明确 UI,不静默覆盖或丢弃用户修改。
- File / Edit / Diff 三态交互无回归;`tn-editor` 核心逻辑必须有 headless 单测覆盖。

## Agent Host 平台化:从 Claude/Codex 特判迁移到可扩展 Agent Runtime ✅(2026-06,P0–P6)

**已完成。** 把 Tn 从「Claude/Codex 当固定类型、特判散落全 UI」重构成对具体 agent **零知识**的 **Agent Host 平台**:Claude/Codex 降为可移除的内置 provider,未来任意 agent(Gemini/Aider/OpenCode/Qwen/自研/远端)按同一套宿主模型进入。**闭合 `AgentKind` 枚举已从全仓删除**,身份统一为开放的 `AgentId`。

### 做了什么(总体)

新增 headless 平台 crate `tn-agent`(对具体 agent 零知识),把「身份 / 能力 / 运行位置 / 事件 / 适配 / 注册」抽象成平台原语;Claude/Codex 的私有日志解析下沉为 `tn-ai` 的**内置 adapter**;`tn-ui` 不再认任何具体 agent,全部经注册表(gpui Global)解析 descriptor。层次 `tn-config ← tn-agent ← tn-ai ← tn-ui` 无环,平台/UI 对 agent 零知识、认 Claude/Codex 的代码只准在 `tn-ai`。

### 已实现(按阶段,每阶段独立编译 + 测试绿)

- **P0 `tn-agent` 平台基座**:`AgentId`(开放字符串身份)· `AgentDescriptor` + `AgentCapabilities`(能力插槽:terminal/usage/transcript/cwd_sync/git_diff/permission_prompts/tool_calls/checkpoints)+ `AgentRuntimeKind` · `AgentEvent` + `AgentStatus`(UI 唯一输入契约)· `AgentAdapter` trait + `SessionRef` · `AgentRegistry` · `AiUsage` + pricing(通用用量契约,从 tn-ai 上移)。
- **P1 内置 adapter**:`ClaudeAdapter`/`CodexAdapter`(住 `tn-ai`,薄包装 `claude.rs`/`codex.rs` 纯解析器)+ `builtin_registry()`;`detect.rs` 泛化为 `resolve_pane_session(&dyn AgentAdapter)`——沿用「launch 后 stale→fresh」绑定(去全局猜测),第三个 agent 无需新 match 臂。
- **P2 config 开放**:theme `[agents.<id>]` 色表(`accent_for`)+ `[general.billing]` 按 id billing 覆盖(`billing_for`),保留旧 `claude/codex` 字段向后兼容。
- **P3 UI 去特判**:`launch`/`welcome`/`header`/`io`/`usage_display`/`layout`/`mod`/`workspace`/`quick_terminal` 全部从 `match AgentKind` 改为 `AgentId` + 注册表 descriptor;per-pane 缓存 `agent_accent`/`label`/`short`/`manages_cursor`/`caps`,只在 agent 变更时经 `resolve_agent_view`(descriptor + theme + adapter)重算,render 路径只读缓存;`force_hide_cursor` → descriptor `manages_own_cursor`;状态栏按 `AgentId` 聚合(无固定 Claude/Codex 槽)。
- **P4 AgentEvent 归约器 + capability 插槽**:用量轮询经 `reduce_agent_event(AgentEvent::UsageUpdated)` 入账(**复用既有后台轮询线程,不新增帧泵,热路径不变**);header 用量环 gate `caps.usage`、活动栏「本次改动」gate `caps.git_diff`——无能力的 agent 隐藏对应区而非空着。
- **P5 运行位置 + 配置级接入**:`LaunchSpec::runtime()` 从 ssh/file_namespace 派生 `AgentRuntimeKind`(PTY 家族),**与 `FileNamespace` 严格分离**;config `[[agents]]` manifest(`AgentManifest` → `AgentDescriptor::from_manifest`)+ `GenericAdapter`(有身份无遥测)+ `register_manifest`(已注册 id 则 no-op,内置 telemetry 优先)——用户写 TOML 即可让新 agent 进启动器/头/能力插槽。
- **P6 剔除内置 + 守卫**:默认 `run()` 不再调 `builtin_registry()`,只注册 config `[[agents]]` → **出厂 = 纯 shell 宿主**;删尽 `AgentKind` 闭合枚举 + 全部 kind-dispatch 死 API;守卫测试 `agent_host::guard::ui_has_no_closed_agent_enum` 扫 `tn-ui/src` 锁死「UI 不得重新引入闭合枚举」。
- **P7 应用内 Agent 编辑器(2026-06,补齐配置级接入的 UX)**:配置级接入原本只能**手编 `config.toml`**(用户反馈:太极客)。新增欢迎页 launchpad「+ 添加 Agent」磁贴 → 居中玻璃浮层(`workspace::render_agent_form`)收 名称/命令/颜色/Ink 自绘光标 → `tn_config::append_agent`/`append_profile`(块级保注释)写 `[[agents]]`+`[[profiles]]` → `reload_agents` 重读盘 + 重装 `AgentRegistry` global + 重建 launchpad **即时生效**(无需重启)。自定义磁贴 hover ✎/✕ 编/删(`remove_agent`/`remove_profile`)。名称字段中文 IME 复用 `EntityInputHandler`(与 SSH 重命名多路复用);`slugify` 派生稳定 `AgentId`、命令首词进 aliases(敲它即切 agent 态)。诚实:config-only = generic,**不给 usage 能力勾选**(无 adapter 即无数据)。明细见 [CHANGELOG](../CHANGELOG.md),坑见 [CLAUDE.md](../CLAUDE.md)。

> 分层接入深度(已就位前两层):① **配置级**(`[[agents]]` 声明即启动,无遥测;**P7 后有应用内编辑器,不必手编 TOML**)✅ ② **观测级**(adapter 读日志/usage,如内置 Claude/Codex)✅ ③ wrapper/sidecar 结构化事件 🧭 ④ 原生 Tn Agent Protocol 🧭。

### 还没实现(后续,低优先)

- **外部进程 adapter 的实时通道**:`AgentAdapter` trait + `GenericAdapter` + manifest 已是**契约与最小实现**;但 stdio/JSON-RPC **实时客户端**(拉起外部进程取 usage/transcript/tool-call/permission)+ manifest 的**网络权限默认拒绝 / 用户确认**安全模型,待有真实外部 agent 可测时实装。
- **非 PTY 运行时**:`AgentRuntimeKind` 枚举已对 RemoteDaemon/Http/WebSocket/Structured 开放,但本轮只产 PTY 家族,未实装非终端运行时。
- **AgentEvent 的高级渲染槽**:`StatusChanged`/`TranscriptAppended`/`PermissionRequested`/`ModelChanged` 等变体已在契约里,但归约器目前是 no-op——尚无对应 UI 槽(状态药丸 / transcript 面板 / 权限确认卡 / Inspector)。
- **遗留清理(可选)**:`tn-ai` 仍 pub 暴露 `claude::`/`codex::` 原始解析函数(adapter 内部在用),非死代码但属内部细节。

### 如何验证

**1. 自动化(无 GUI)**

```powershell
cargo test --workspace --lib     # 全绿:tn-agent 9 / tn-ai 17 / tn-config 35 / tn-core 31 / tn-pty 15 / tn-shell 13 / tn-blocks 5 / tn-ui 80
cargo test -p tn-ui guard        # 守卫:UI 零闭合 AgentKind 枚举
cargo test -p tn-agent           # 注册表命令匹配 / manifest→descriptor / capability / register_manifest 不覆盖内置
$env:TN_AUTOQUIT="1"; cargo run -p tn-app   # 空注册表 headless:pane 起、网格收敛、exit 0
```

**2. 肉眼 · 出厂纯 shell 宿主(验证「真剔除」)** — `cargo run -p tn-app`:默认注册表空 → 敲 `claude`/`codex` 不再自动切 agent 态;点 claude 磁贴以 **generic agent** 托管(有活动栏,**无珊瑚 accent / 无用量环**)。"能跑但无 Claude 专属颜值/用量" = 内置已剔除、平台不依赖它们。

**3. 肉眼 · 把 agent 加回来(验证平台通用 + 回归)**
- **应用内编辑器(推荐,P7)**:`cargo run -p tn-app` → 新标签(`Ctrl+Shift+T`)→ launchpad 点「+ 添加 Agent」→ 填 名称 `Gemini CLI`、命令 `gemini`、选颜色、Ink 开关 → 点「添加」。应**立刻**出现一张新 agent 磁贴(无需重启);`config.toml` 多出 `[[agents]]`+`[[profiles]]` 两块(原注释保留)。点磁贴起会话、或在普通 shell 敲 `gemini` 都应切出该 accent 的 agent 头(generic = 有活动栏、无用量环)。磁贴 hover ✎ 改名/改色即时生效、✕ 删除即抹掉两块。名称填中文(如 `通义千问`)验 IME。
- **配置级(改 TOML)**:`%APPDATA%\Tn\config.toml` 加 `[[agents]]\nid="gemini"\nlabel="Gemini CLI"\naliases=["gemini"]\naccent="#4488ff"\ncapabilities=["usage"]`,重启后 shell 敲 `gemini` → 切出蓝色 agent 头 + 活动栏(身份来自 manifest),证明新 agent 零代码进平台。
- **恢复内置全功能(改一行)**:[crates/tn-ui/src/lib.rs](../crates/tn-ui/src/lib.rs) `run()` 里 `let mut registry = tn_agent::AgentRegistry::new();` → `tn_ai::builtin_registry();`,跑后 Claude/Codex 的珊瑚/青色 accent + 用量环 + billing + 敲命令切态 + 退出还原应全部如旧(只是现在全经 `AgentId`+registry,不再特判)。验完改回即恢复出厂态。

**4. 运行时诊断(可选)**:`$env:RUST_LOG="tn=debug"` 跑,每开 pane 打 `spawning pane … runtime=LocalPty/WslPty/SshPty namespace=…`,验证 runtime ≠ namespace 分离。

> ⚠️ debug 跑的 `tn.exe` 锁 `target\debug\tn.exe`,改完代码重新 `cargo run` 前先关旧实例(否则重链「拒绝访问」)。真机肉眼(剔除前后 agent 颜值/用量/切态)仍建议你本机过一遍——headless 只验了网格收敛与无 panic。

逐项明细见 [CHANGELOG.md](../CHANGELOG.md) Agent Host 段;架构与边界见 [架构蓝图.md](架构蓝图.md) §4.6;操作指南 + 踩坑见 [CLAUDE.md](../CLAUDE.md) tn-agent/tn-ai 条 + 「Agent Host 平台化」坑。

## 对面板进行解耦

目标是让每个终端窗格拥有自己的“工作区上下文”,而不是让左侧文件树、Agent 改动列表、用量统计都依赖一个全局状态。

- **每个 pane 绑定自己的文件上下文**:每个 Host / WSL / SSH pane 都应记录自己的文件命名空间、当前 cwd、对应的 Explorer root。切换焦点 pane 时,左侧文件树显示该 pane 对应的文件系统视图;切回其它 pane 时恢复它自己的目录状态、展开状态和选中文件。
- **Agent 信息跟 pane 绑定**:Claude / Codex 这类 agent pane 的“本次改动”、git diff、用量环、会话状态都必须归属到具体 pane,不能被其它 pane 的 cwd 或新启动的 agent 会话串台。
- **左上角「打开文件夹」按当前 pane 的命名空间执行**:
  - Host / Windows pane:继续使用系统原生文件夹选择器,选择 Windows 路径后更新当前 pane 的 Explorer root,并让对应本地 shell `cd` 到该目录。
  - WSL pane:用户应能像打开 Windows 文件夹一样浏览 Linux 目录,但实际显示的是 `\\wsl$\<distro>\...` 映射出的 Linux 文件树;选择目录后要转换回 Linux 路径,让对应 WSL bash pane `cd` 到该目录。
  - SSH pane:需要先实现 SFTP / 远端文件服务。完成后,「打开文件夹」应打开远端目录选择器或远端 Explorer,显示远端 Linux/macOS 目录;选择目录后让对应 SSH shell `cd` 到远端路径。
- **同步范围要收敛到目标 pane**:用户对某个 pane 打开的文件夹或执行的目录切换,默认只影响该 pane 及其绑定的文件树/Agent rail;不要无差别把 `cd` 发送给所有 pane,尤其不能发送给 Claude/Codex 交互中的 agent pane。
- **命名空间边界不能混淆**:Windows `PathBuf`、WSL Linux 路径、SSH 远端路径必须显式转换。没有远端 FS 后端时,SSH cwd 只能作为终端状态展示,不能驱动本机 Explorer / Quick Look / git watcher。

## 对每一页面的样式重新设计，增加更多细节，尝试使用更高级的界面库或者自研界面库

# 已知限制 / 待修(非路线图项)

> 这里只记**当前真实存在、但够不上里程碑级别**的小限制 / 待办 / parked。
>
> - **里程碑级路线图** → [架构蓝图 §8](架构蓝图.md)(含 M2 SSH parked、自定义 `TerminalElement`、拖拽停靠、配色导入等)。
> - **踩过的坑(已修 + 教训)** → [CLAUDE.md](../CLAUDE.md)「踩过的坑」。
> - **逐项优化明细** → [优化日志.md](优化日志.md) / [CHANGELOG.md](../CHANGELOG.md)。
>
> 修掉一项就从这里删,别让它变回一潭旧账。

## Quick Look 速览 / 编辑器

- **散文 / Markdown 长段落不能软换行**:固定行高 `uniform_list`(只 measure row 0)→ wrap 会变高破坏虚拟化,长段落只能横向滚。prose 体验欠佳。要 wrap 得放弃该场景虚拟化或改测每行高。
- **Diff tab 不支持拖选**:鼠标拖选只做了 File / 编辑态;Diff 行未接。
- **编辑态每键 `Rc::make_mut` 深拷整 buffer**:412 行亚毫秒、非冻结主因,但接近 `MAX_LINES`(4000)会有延迟感;真要治改 `Rc<RefCell<Vec<String>>>`。

## 输入 / 键位

- **命令面板中文搜索 parked**:面板靠 `on_key` 读 ASCII,IME 合成的中文只经 `EntityInputHandler` 投递、面板未接 → 目前只能搜英文。补法见 CLAUDE.md「未做/后续」。
- **`Ctrl+Shift+*` 可能被中文 / 多布局 Windows 的 IME / 系统热键吞**:真机不达时可改键,或把系统「输入语言热键」设未分配。
- **IME 合成键子类化修复(`VK_PROCESSKEY`)真机待验**:理论成立(IMM32 标准),须真机肉眼 + 看日志(`RUST_LOG=tn::ime=debug`)确认微软拼音确以 `VK_PROCESSKEY` 投递合成键。

## 真机肉眼待验(headless 验不了)

- 颜值微调 · 标题栏拖动/控制点 · Quick Terminal 动画顺滑 / 多显示器 / 高 DPI 定位 / 首帧不空白(M5 真机验待 owner)。
- per-pane cwd 经 OSC 7 实时跟随。

## 待接数据源

- **欢迎页「最近目录」**:需 recent-sessions 数据源(agent 会话 cwd + mtime)。

## 远端 / WSL 文件系统

- **SSH / macOS 远端文件树 + 编辑首版已接 SFTP** ✅:`tn-pty::remote_fs` 提供 SFTP v3 `RemoteFileService`(列目录 + stat + 有界读 + 写文件);`ExplorerRoot::Ssh` / `ExplorerPath::Remote` 用 `RemoteId` 表示远端路径,不伪装成本机 `PathBuf`;焦点切到 SSH pane 时 Workspace 可把远端 cwd 映射成远端 Explorer root,文件树通过 SFTP 枚举。「打开文件夹」在 SSH pane 内打开应用内远端目录 picker,确认后只向目标 pane 发远端 `cd`。Quick Look 通过 SFTP 做有界预览和文本编辑写回,保存前用 stat + 内容 hash guard 检测远端外部变化,冲突时必须显式选择「重新载入 / 取消 / 覆盖远端」。远端 git 活动栏 / full diff 走 `tn-pty::remote_cmd` + `remote_git` 数据模型,不跑本机 git / notify。远端 hunk accept/reject 可视按钮 + 刷新 + 失败提示已落地(2026-06-07,见上「面板解耦」块)。仍未做:SFTP/SSH 端到端实机回归。
  - **🐞 已修(2026-06-07):SSH 文件树/「打开文件夹」一直停在 Windows 主机目录 = 远端 cwd 永远拿不到**。根因:SSH 注入的提示符脚本**只发** `OSC 633;P;Cwd`(不含 A/B/C/D block 标记),而 `TerminalView::cwd()` 只读 `BlockModel::current()`/`last_finished()` 的**逐 block** cwd——裸 `P;Cwd` 不建 block,故 `current()`/`last_finished()` 恒 None → `cwd()` 恒 None → `explorer_root_from_parts` 的 Ssh 分支 `cwd.filter(starts_with('/'))?` 返回 None → Explorer 不 re-root、「打开文件夹」也拿不到 root 不开远端 picker(两症同根)。本地 pwsh 不受影响(它发全套 133;A/B/C/D,block 携带 cwd)。**修两处**:① `cwd()` 增补 `BlockModel::cwd()`(模型级 cwd)兜底,裸 `P;Cwd` 也能浮出;② 注入脚本在 `PROMPT_COMMAND`/`precmd` 钩好后**立即调一次** `__tn_pc`,连接当下即报 cwd(不等下个提示符)。headless 单测全绿(`explorer_root_from_parts_maps_ssh_cwd_to_remote_root` 等),**真机点连接 → 文件树应自动切到远端目录、「打开文件夹」应弹远端 picker 待 owner 肉眼确认**。
  - **🐞 已修(2026-06-07):远端目录 picker 无法切到别的目录(/root 进不去 /usr·/mnt)+ 列表被裁切/不滚动**。根因有二:① picker 的目录行 `on_mouse_down` 只能**往下进**子目录,**没有任何鼠标「上级」入口**(只有键盘 `Backspace`/`Left`),用户被困在当前子树;② 列表用 `max_h(320)+overflow_hidden` **死裁**,`/` 几十个条目看不全、也不滚 → 键盘 `↑↓` 移动到折叠区**不可见**,体感「按键无效」。修:① 列表顶部加**可点击的「`..` 上级目录」行**(`current.parent()` 存在即显示,固定在滚动区上方)→ 鼠标即可 `/root → / → /usr`;② 目录列表改 **`uniform_list` 虚拟化 + `track_scroll`**(高 = min(内容, 320),鼠标滚轮可滚),键盘 `↑↓` 配 `scroll_to_item(Center)` 让选中项始终可见。键盘 nav(`↑↓`/`Enter`/`Backspace`/`Esc`/`Ctrl+Enter`)与命令面板同一焦点范式(每帧重抢焦点)。
  - **🐞 已修(2026-06-07,真机确认真凶):picker 键盘完全无反应(↑↓/Enter/Esc/Backspace 全死)= `render` 里的「焦点反射」块每帧把焦点从 picker 抢回 `workspace_focus`**。根因:`Workspace::render` 有一段「从 gpui 实际焦点反推被选中 pane」的逻辑,gate 在 `overlay_focused`(开着的浮层列表)——但这个列表**漏了 `remote_dir_picker`**。于是 picker 开着时该块照跑:发现「没有 pane 持焦点」(焦点在 picker 上)→ 走 else 分支 `workspace_focus.focus(window)` **每帧重锚到根**,把 reveal 块刚给 picker 的焦点**抢走** → picker 的 `track_focus` 永不持焦 → `on_key_down` 永不触发 → 键盘全死。命令面板等之所以好,正因它们**在 `overlay_focused` 列表里**、这段被跳过。修:把 `self.remote_dir_picker.is_some()` 加进 `overlay_focused`。**通则:任何持自己焦点句柄的浮层,必须同时进 `render` 的「reveal 聚焦块」**和**「焦点反射块的 `overlay_focused` 排除项」,只进前者会被后者每帧抢走。**(待补进 CLAUDE.md gpui 焦点坑。)
  - 附带(非此 bug 主因,但正确):`disable_ime` 也补了 picker / split / layout / palette —— 这些无 `EntityInputHandler` 的导航浮层不该开 IME(避免活动 CJK IME 把 `VK_PROCESSKEY` 导航键经 `install_ime_keyfix` 子类吞掉)。两道一起保导航键活。
  - **🐞 已修(2026-06-07):WSL「打开文件夹」开的是 Windows 原生选择器(默认停 Windows 盘、够不到 Linux 目录)**。修:**把应用内目录 picker 泛化成支持 WSL**——`PickerSource::{Ssh,Wsl}` + `PickerEntry`(解耦 SSH 专属 `RemoteId`),WSL 经 `\\wsl$\<distro>` 本地 UNC 用 `std::fs` 列目录(`list_wsl_dir`),`确认` 时建 `ExplorerRoot::wsl`。`open_folder_should_use_native_picker` 现只对 **Host / 欢迎页**返回 true;SSH 与 WSL **都走应用内导航 picker**(上级/进入/确认一致)。cwd 未知时 `fallback_remote_root` 兜底从 `/` 起,picker 必能打开。headless 单测全绿(`wsl_source_label_shows_distro`、picker nav 等),**真机待 owner 肉眼确认**。
- **WSL 已走独立 Explorer root**:WSL 启动不继承 Windows cwd,文件树跟随时由 `ExplorerRoot::Wsl` 显式映射到 `\\wsl$\<distro>\...` 并驱动 Linux 目录树,而不是 `C:\`/`D:\`。「打开文件夹」现也走应用内 WSL picker(见上)。

## Tn Editor 用户体验升级:从 Quick Look 快编到可扩展编辑器

> 本条分两层:**上半 = 愿景稿(为什么/要什么)**,**下半 = 严格可执行的任务规划(怎么做/怎么验)**。改动看下半,理解取向看上半。

---

## 〔愿景稿〕

当前 Quick Look 编辑器已经能完成“点开文件、快速修改、保存”的闭环,但它仍然是一个塞在 `quick_look.rs` 里的轻量快编器。继续直接堆软换行、多光标、Diff 拖选、LSP、打字动画,会让用户体验变得不可预测:有些场景很漂亮,但保存不安心、长文件不流畅、远端文件不可用、动画还可能拖慢输入。

这项优化的目标不是把 Tn 变成 VS Code,而是把编辑能力升级成 **Tn 工作流里的可信编辑器**:打开快、编辑稳、保存安全、审查 agent 改动顺手,动画作为细节增强而不是主目标。

**用户视角目标**

- **不离开上下文**:用户在终端、文件树、agent 改动卡片之间流转时,能直接预览、编辑、保存,不需要跳到外部编辑器。
- **不怕误改和丢数据**:编辑前后有 dirty 状态、撤销/重做可靠、保存前能发现磁盘外部变化,关闭浮层/切文件时不会静默丢修改。
- **读得舒服**:代码长行继续横滚,Markdown/日志/说明文档应支持软换行;大文件要能快速只读打开,不能因为一个文件拖垮 UI。
- **改得顺手**:鼠标选区、键盘选择、查找替换、复制粘贴、Diff 查看和快速保存都要统一;未来多光标/列选/LSP 要有清晰落点。
- **反馈高级但不打扰**:打字动画、caret 过渡和插入字符反馈应该让输入更有质感,但任何时候都不能牺牲“光标真实位置立即到位”和“连续输入不卡”。

**产品形态**

- **Quick Look 快编**:仍是默认入口。文件树 `Space`/`Enter`、agent 改动卡片、设置文件都先打开浮层。适合看文件、小改动、配置修改、快速 Diff。
- **Editor Pane**:当用户需要长时间编辑或多文件处理时,Quick Look 可升级为正式编辑 pane/tab。它共享同一编辑核心,但拥有更稳定的空间、状态栏和后续 LSP 能力。
- **Diff Review**:agent 改动卡片进入审查视图。短期支持 Diff 拖选/复制/跳转;中期支持 hunk 级 accept/reject、打开对应文件定位编辑。

**架构方向**

- 新增 headless `tn-editor` 层,把编辑核心从 `quick_look.rs` 中拆出。它不依赖 GPUI,负责 `Document`、`CursorSet`、`Selection`、`EditTransaction`、`UndoStack`、`SearchState`、`LineLayout`、编码/换行符、dirty 状态和保存冲突检测。
- UI 层新增 `editor::element` / `QuickLookCodeElement`,用 GPUI `Element` 的 `layout/prepaint/paint` 统一绘制文本、行号、选区、搜索命中、diff decoration、IME preedit、caret 和 typing effects。
- Quick Look、Editor Pane、Diff Review 都消费同一 `tn-editor` 文档模型和同一 renderer。Quick Look 只是宿主形态之一,不再拥有完整编辑业务。
- 文件访问继续受命名空间约束:Host 可直接读写;WSL 走 `\\wsl$` 映射;SSH/macOS 远端走 SFTP 远端 FS 后端,不能把远端路径伪装成本机 `PathBuf`。

**编辑体验要求**

- **保存安全**:打开文件时记录 mtime/size/hash;保存前若磁盘已变化,提示用户选择覆盖、重新加载、另存为或查看差异。默认不做静默覆盖。
- **关闭安全**:Quick Look 关闭、切文件、切 Diff、退出应用时,若当前文档 dirty,必须给出保存/放弃/取消。小改动也不能静默丢。
- **换行与编码**:保留原文件换行符风格和编码信息。无法可靠识别时明确降级为 UTF-8 文本编辑或只读预览。
- **大文件策略**:小文件可编辑;接近阈值时提示性能风险;超大文件默认只读流式/分页预览。用户需要强制编辑时必须明确确认。
- **软换行策略**:代码默认横滚;Markdown、txt、log、说明文档默认软换行。实现上采用 logical line -> visual line 映射,视觉行仍固定行高,避免破坏虚拟化。

**打字动画纳入范围**

打字动画保留,但它是 renderer 的可选 effect,不是编辑核心能力。

- 逻辑 cursor、selection、undo、文本内容必须在输入事件提交时立即更新;动画只能表现视觉过渡。
- 允许的效果:同一视觉行内 60-90ms caret 轻微 glide,块光标 squash/pop,新插入字符 70-110ms 的轻微亮度/alpha settle。
- 禁止的效果:不允许在 `uniform_list + div/span` 外层用绝对叠层和 16ms 帧泵推动光标;不允许积累 per-char 动画队列;不允许让动画改变文本宽度、行高或真实命中测试。
- 自动 snap 场景:IME composition、拖选、选区存在、查找跳转、PageUp/Down、跨行移动、滚动、文件切换、窗口失焦、大文件、高帧耗时。
- 配置项:提供 `editor.animations = "off" | "subtle" | "full"` 或 reduced-motion 开关。默认建议 `subtle`;性能不达标时自动降级到当前瞬时反相块。

**渲染方案**

- `EditorElement::prepaint` 计算可见 visual lines、行号槽、文本区、caret rect、选区 rect、IME candidate bounds 和 scrollbar geometry。
- `EditorElement::paint` 只绘制可见区域。文本 shaping 缓存按 `(line_id, line_revision, theme_revision, font_key, wrap_mode)` 失效。
- 固定单元格模型保留:ASCII 1 列,CJK/宽字符 2 列。软换行在 visual line 层做,不改变字符命中模型。
- Diff 视图作为 decoration 渲染,不单独维护另一套文本控件。File / Edit / Diff 三态共用选择、复制、滚动和命中测试。

---

## 〔执行计划稿〕

> **下半 = 面向后续编码会话的任务手册**。目标与取向见上半愿景稿,此处只管「怎么做、怎么验、什么算完成」。本节按 **200k 上下文执行包** 重划:一次会话只认领一个 `TnE-*` 包,做完验证并同步文档后再进入下一个包;不得把相邻包顺手合并。

### 0. 200k 上下文执行协议

- **最小认领单位**:`TnE-*` 是后续实现的最小任务包。每包必须能在一个 200k 上下文窗口内完成:读入、实现、验证、文档同步都包含在内。
- **预算硬线**:每包总上下文目标 ≤ 180k,留下至少 20k 处理意外。单包读入 ≤ 110k,实现/编辑推理 ≤ 45k,验证与收尾 ≤ 25k。超过任一项就先拆包,不继续编码。
- **读入顺序**:每包先读 `AGENTS.md`、`CLAUDE.md` 对应坑位、本节、任务卡列出的文件。`quick_look.rs` 这类大文件只按 `rg` 定位切片读,禁止整文件塞满上下文。
- **改动上限**:单包最多新增 1 个 crate 或 1 个 UI 子模块;最多实质修改 6 个源码文件 + 必要文档。若需要同时碰核心模型、renderer、workspace 生命周期,必须拆成两个包。
- **行为约束**:除任务卡明确允许的行为变化外,Quick Look 既有快捷键、鼠标选区、保存、Diff、IME、CJK 命中、滚动心智必须零回归。
- **验证底线**:每包至少跑任务卡列出的最小验证。涉及 headless 逻辑跑对应单测;涉及 UI 跑 `cargo test -p tn-ui --lib`;涉及新 crate 或依赖方向跑 `cargo test --workspace --lib` 和 `cargo build --workspace`。
- **文档同步**:每包完成后更新本节状态;若改变架构边界,同步 `CLAUDE.md` 和 `docs/架构蓝图.md`;若改变用户可见 UX,同步 `docs/产品设计.md`;若清掉待修项,删本文件对应旧条。
- **拆分触发**:遇到 IME、远端 FS、Diff apply、性能动画、workspace pane 生命周期任一方向的未知坑,先停在当前包内写下发现,拆出新 `TnE-*` 包,不要在同一上下文里继续扩大半径。

### 1. 现状锚点(动手前先确认仍成立)

> 改前先核对这些符号还在原位。若漂移,第一步只更新锚点和任务卡,不要带着过期行号编码。

- 单体文件 [quick_look.rs](crates/tn-ui/src/quick_look.rs)(当前约 4.9k 行)仍持有 Quick Look 预览、编辑、Diff、保存和渲染业务。
- 入口与状态:`Tab` 在 `quick_look.rs:78`;`QuickLookEvent` 在 `quick_look.rs:308`;`QuickLook` 在 `quick_look.rs:714`;`workspace.rs` 持有 `quick_look` 实体和 open/close/nav/file-saved 订阅。
- 编辑状态:`buf: Rc<Vec<String>>`(`quick_look.rs:730`)、`cursor`(`:732`)、`sel_anchor`(`:734`)、`undo/redo` 快照栈(`:741/:742`)、`dirty`(`:746`);当前仍有每键 buffer 快照/深拷风险。
- 保存路径:`save_state_after_success`(`quick_look.rs:492`)、`local_file_guard`/`save_local_text`(`:604/:615`)、本地 `save`(`:1715`)、远端 `save_remote`(`:1813`)、`QuickLookEvent::FileSaved` 发射点在 `:1752/:1800/:1892` 附近。
- 可迁移纯函数:`highlight`(`quick_look.rs:202`)、`parse_diff`(`:2612`)、`char_to_byte`(`:2729`)、`op_insert`/`op_newline`/`op_backspace`/`op_delete`/`op_move`/`op_page`/`op_delete_range`/`op_insert_multiline`(`:2744-2883`)、`all_matches`(`:2913`)。
- 渲染锚点:`uniform_list` 路径在 `quick_look.rs:3538` 和 `:3633`;横滚字段 `hscroll_px` 在 `:806`;横滚/命中说明在 `:3859` 之后。当前散文/Markdown 不能软换行。
- 关联坑(实现时必读 [CLAUDE.md](../CLAUDE.md)):同步 git 冻 UI、`uniform_list` 固定行高、caret-follow 去抖、帧泵动画卡顿、IME `EntityInputHandler` + `VK_PROCESSKEY`、自绘滚动条命中区 + `pressed_button` 兜底、Quick Look click-away/focus 规则。

### 2. 任务总览

| 包     | 阶段 | 可交付目标                                           | 预算上限 | 依赖                        | 状态 |
| ------ | ---- | ---------------------------------------------------- | -------: | --------------------------- | ---- |
| TnE-00 | 基线 | 锚点校准 + golden/回归测试缺口补齐                   |     120k | 无                          | ✅    |
| TnE-01 | P0   | 本地文件守卫纯逻辑 + 换行/编码探测                   |     145k | TnE-00                      | ✅    |
| TnE-02 | P0   | 保存冲突检测接入本地保存路径                         |     155k | TnE-01                      | ◐ headless,真机待验 |
| TnE-03 | P0   | dirty-close 守卫覆盖关闭/切文件/切 Diff/退出         |     160k | TnE-02                      | ◐ headless,真机待验 |
| TnE-04 | P1   | 新建 headless `tn-editor` crate + 文本编辑纯函数迁移 |     165k | TnE-03                      | ✅    |
| TnE-05 | P1   | selection/search/undo transaction 模型迁移           |     170k | TnE-04                      | ✅    |
| TnE-06 | P1   | Quick Look 薄壳接入 `tn-editor::Document`            |     175k | TnE-05                      | ◐ headless,真机待验 |
| TnE-07 | P1   | 去除每键整 buffer 深拷和性能回归验证                 |     150k | TnE-06                      | ◐ headless,真机待验 |
| TnE-08 | P2   | `EditorElement` 只读几何/布局模型                    |     160k | TnE-06                      | ✅ headless |
| TnE-09 | P2   | File 预览态 env-gated 接入只读 renderer              |     175k | TnE-08                      | ◐ prepaint 就绪,paint+真机待 |
| TnE-10 | P2   | 只读选区/复制/CJK 命中 parity,下线预览旧路径         |     170k | TnE-09                      | ✅ 真机验(选区/复制/CJK/横滚)+ 默认翻转 |
| TnE-11 | P3   | 编辑态输入 transaction + caret/selection 绘制        |     175k | TnE-10                      | ◐ 自绘编辑态落地,真机待验 |
| TnE-12 | P3   | IME/preedit/鼠标拖选/滚动跟随 parity,下线编辑旧路径  |     180k | TnE-11                      | ☐    |
| TnE-13 | P4   | Diff decoration 模型 + 只读 Diff renderer            |     160k | TnE-10                      | ◐ 模型就绪,renderer+真机待 |
| TnE-14 | P4   | Diff 拖选/复制/hunk 跳转                             |     170k | TnE-13                      | ☐    |
| TnE-15 | P5   | `LineLayout` logical→visual headless 模型            |     160k | TnE-12                      | ✅ headless(提前) |
| TnE-16 | P5   | 软换行接入 File/Edit 渲染与命中                      |     180k | TnE-15                      | ☐    |
| TnE-17 | P6   | `[editor]` 配置 schema + motion policy               |     140k | TnE-12                      | ✅ headless(提前) |
| TnE-18 | P6   | subtle typing effects + perf 降级 gate               |     175k | TnE-17                      | ☐    |
| TnE-19 | P7   | 文档会话共享 + Quick Look 升级入口                   |     170k | TnE-12                      | ☐    |
| TnE-20 | P7   | Editor Pane UI/状态栏/生命周期                       |     180k | TnE-19                      | ☐    |
| TnE-21 | 收尾 | 全局回归、待修清理、架构/产品文档同步                |     130k | TnE-14/TnE-16/TnE-18/TnE-20 | ☐    |

### 3. 执行包明细

#### TnE-00 — 基线锚点与回归测试补位

- **状态(2026-06-08)**:✅ 已完成。现有 `quick_look.rs` 已覆盖 op/search/diff parse/CJK hit-test/save-state 等基线;本轮新增本地 guard / 保存冲突红绿测试后,`cargo test -p tn-ui --lib` 通过。
- **目标**:不改产品行为,只建立后续迁移能依赖的基线。
- **读入**:`quick_look.rs` 的锚点切片、`workspace.rs` Quick Look 订阅切片、现有 `quick_look.rs` tests、`CLAUDE.md` Quick Look/IME/焦点坑位。
- **改动范围**:允许补 `quick_look.rs` 内现有纯函数测试或 test helper;不引入新架构。
- **验收**:记录当前 op、search、diff parse、CJK byte/char 转换、save-state 的测试覆盖缺口;能补的先补齐。
- **验证**:`cargo test -p tn-ui --lib`。
- **禁止**:不改 UI,不改保存行为,不移动函数。

#### TnE-01 — 本地文件守卫纯逻辑

- **状态(2026-06-08)**:✅ 已完成。`quick_look.rs` 新增 `local_file_guard`、`decode_text_bytes`/`encode_text_lines` 复用路径;本地打开会记录 `TextFormat` 和 `FileGuard`,保留 LF/CRLF、UTF-8 BOM、UTF-16LE/BE、GBK 等文本格式信息。
- **目标**:为本地文件建立 `FileGuard`、换行符探测、编码/只读降级判断的纯逻辑。
- **读入**:`quick_look.rs:652-810` 状态字段、`:1564` 本地保存路径、`crates/tn-ui/Cargo.toml` 现有 `encoding_rs`/`content_inspector` 依赖。
- **改动范围**:只在 Quick Look 本地文件模型附近新增 guard 数据结构和纯函数;不弹 UI,不改变保存结果。
- **验收**:LF/CRLF 保留、mtime/size/hash 采样、无法安全编辑时的判定都有单测。
- **验证**:guard/newline/encoding 单测 + `cargo test -p tn-ui --lib`。
- **禁止**:不碰远端 SFTP 保存语义;不把远端路径转成本机 `PathBuf`。

#### TnE-02 — 保存冲突检测接入

- **状态(2026-06-08)**:◐ headless 已完成,真机待验。本地 `save()` 已接入 `save_local_text`:保存前按打开时 `FileGuard` 对比当前磁盘,冲突进入统一操作条「重新载入 / 取消 / 覆盖保存」,默认不覆盖;保存写回保留原编码和换行风格。已由单测覆盖本地外部修改拒写与 force 覆盖,仍需真机用外部编辑器改同文件后走 UI。
- **目标**:把 TnE-01 的 guard 接入本地 `save`,外部修改时不静默覆盖。
- **读入**:`quick_look.rs:1438` 惰性 Diff、`:1564-1604` 本地保存、`:476` save state、workspace 的 `FileSaved` 刷新订阅。
- **改动范围**:本地保存前检测冲突;新增最小冲突状态和操作分支:覆盖、重新加载、取消/留在当前文档。另存为/查看差异可先留明确禁用入口,不得假装完成。
- **验收**:外部改同文件后 Ctrl+S 必须进入冲突状态;默认按钮不是覆盖;选择取消不丢本地修改。
- **验证**:冲突状态单测 + `cargo test -p tn-ui --lib`;真机用外部编辑器改同文件后保存。
- **禁止**:不做 renderer 重构;不引入异步 git 调用到 UI 热路径。

#### TnE-03 — dirty-close 守卫

- **状态(2026-06-08)**:◐ 真机验出一处漏洞并已修。app 内 关浮层/切文件/切 Diff/菜单退出/Ctrl+Shift+Q 均经 `request_close`/`request_quit` 守卫;但**标题栏 ✕ / Alt+F4 走 OS 直接关窗(`window_control_area(Close)` NC 命中),从不经 `request_quit`** → 有未保存编辑时静默丢失(只在用 ✕/Alt+F4 时复现 = 用户报告的「偶尔失效」)。修:workspace reveal 块注册 `window.on_window_should_close` → `quick_look_open` 时返回 `quick_look.request_quit()`(干净放行 / 脏阻止并弹提示,经 `QuitConfirmed`→`cx.quit()`)。仍待真机复验 ✕/Alt+F4 关窗会弹提示。
- **目标**:所有离开当前 dirty 文档的路径都显式确认。
- **读入**:`quick_look.rs` close/nav/tab 切换处理、`QuickLookEvent::Nav/Close`、`workspace.rs:1040-1072` 事件订阅、`:1617` toggle、`:6047` 浮层 scrim。
- **改动范围**:关闭浮层、切文件、切 Diff、退出/隐藏主窗口前,若 dirty 则进入保存/放弃/取消状态;取消留在当前文档。
- **验收**:每条离开路径都有一致守卫;保存成功后原动作继续;放弃后原动作继续;取消无状态丢失。
- **验证**:`cargo test -p tn-ui --lib`;真机逐条走关闭、上下文件、File/Diff tab、应用退出。
- **禁止**:不重构编辑核心;不把 dirty guard 写成 workspace 全局状态。

#### TnE-04 — 新建 headless `tn-editor` 与编辑纯函数迁移

- **状态(2026-06-08)**:✅ 已完成。新增 headless `crates/tn-editor`,注册进 workspace 并由 `tn-ui` 依赖;`char_to_byte`、`line_chars`、insert/newline/backspace/delete/move/page/range/multiline 已迁移为无 GPUI 依赖的编辑核心函数。Quick Look 当前仍保留宿主状态与旧 renderer,但编辑按键路径已调用 `tn-editor` 的纯函数;原 Quick Look op 单测继续通过,`tn-editor` 自身新增 8 个 headless 单测覆盖迁移行为。
- **目标**:创建不依赖 GPUI 的 `tn-editor`,迁移最小文本编辑内核。
- **读入**:workspace `Cargo.toml`, `crates/tn-ui/Cargo.toml`, `quick_look.rs:2498-2638` 的 char/op 函数和相关测试。
- **改动范围**:新增 `crates/tn-editor`;workspace 注册;`tn-ui` 依赖 `tn-editor`;迁移 `char_to_byte`、insert/newline/backspace/delete/move/page/range/multiline 的等价实现和测试。
- **验收**:`tn-editor` 单测覆盖原行为;Quick Look 暂可继续用旧函数或薄转发,产品行为不变。
- **验证**:`cargo test -p tn-editor`;`cargo test --workspace --lib`;`cargo build --workspace`。
- **禁止**:新 crate 禁止依赖 `gpui`;不在本包迁移 renderer/search/undo。

#### TnE-05 — selection/search/undo transaction 模型

- **状态(2026-06-08)**:✅ 已完成。`tn-editor` 已新增 `Document`、`Selection`、`CursorSet`(单主光标)、`TextRange`、`EditTransaction`、`UndoStack`、`SearchState`;当前仍采用与 Quick Look 一致的快照式 undo/redo 语义,连续 `type_text` 合并成一个 undo 步。headless 单测覆盖选区文本/替换、单光标移动与 Shift 扩选、查找命中/选择、replace-all、undo/redo 和最近 before/after transaction。Quick Look UI 已在 TnE-06 通过薄壳适配层接入 `Document`。
- **目标**:把选区、查找、撤销/重做从 UI 状态抽成 headless transaction 模型。
- **读入**:`quick_look.rs:668-684` 编辑状态、`:1729-1930` 选区/复制/移动相关切片、`:2682` `all_matches`、现有 undo/redo 调用点。
- **改动范围**:`tn-editor` 增 `Document`、`Selection`、`CursorSet`(先单光标)、`EditTransaction`、`UndoStack`、`SearchState`;保留未来多光标接口但不实现多光标 UX。
- **验收**:单光标编辑、选区删除/替换、查找命中、undo/redo 都有 headless 单测;语义与 Quick Look 当前一致。
- **验证**:`cargo test -p tn-editor`;`cargo test --workspace --lib`。
- **禁止**:不改 Quick Look UI;不引入 LSP/diagnostics。

#### TnE-06 — Quick Look 薄壳接入 `Document`

- **状态(2026-06-08)**:◐ headless 已完成,真机验出一处 bug 并已修。`quick_look.rs` 新增 `QuickLookEditState` 薄壳,内部以 `tn-editor::Document` 作为编辑态主状态;旧 `uniform_list` renderer 继续读取适配层提供的 `Rc<Vec<String>>` 快照,因此视觉和鼠标命中路径不变。打字、换行、缩进、退格/删除、撤销/重做、全选、剪切/粘贴、查找/替换、保存和远端保存都改为通过 `Document` 更新;预览态只读选区仍保留在 Quick Look 外层状态。**真机 bug:退出编辑(escape)回预览显示旧内容**——预览从 `file_data` 渲染而编辑只改 `Document`,escape 没把缓冲镜像回 `file_data`(要重开文件才刷新);修:新增 `sync_preview_from_edit`,escape 退出时(dirty 才)把缓冲写回 `file_data` 并清行高亮缓存,不写盘不动 dirty。自动验证已通过 `cargo fmt`、`cargo test -p tn-editor/-p tn-ui --lib`;仍需真机复验编辑后退预览即时刷新 + 小文件编辑/保存/查找回归。
- **目标**:Quick Look 编辑态开始消费 `tn-editor::Document`,但视觉和交互保持现状。
- **读入**:`quick_look.rs` 编辑入口、保存、查找替换、复制粘贴、鼠标点位、render 读 `buf/cursor/sel_anchor` 的切片。
- **改动范围**:用 `Document` 替代 `buf/cursor/sel_anchor/undo/redo/search` 主状态;旧 `uniform_list` renderer 仍可继续读适配后的快照。
- **验收**:打字、撤销/重做、全选、复制粘贴、查找替换、保存、鼠标选区逐项无回归。
- **验证**:`cargo test -p tn-editor`;`cargo test -p tn-ui --lib`;真机 Quick Look 小文件编辑回归。
- **禁止**:不碰 `EditorElement`;不做软换行;不做动画。

#### TnE-07 — 去除每键整 buffer 深拷

- **状态(2026-06-08)**:◐ headless 已完成,真机大文件手感待验。复核后确认增量机制随 TnE-05/06 已落地、本轮补齐回归守卫锁死不变量:① `tn-editor::Document` 的 undo/redo 用 `EditTransaction` + 行区间 `EditSnapshot`(`capture_line_span` 只拷受影响行),不存整 buffer;连续打字经 `coalesce`(按 `start_row` 合并)聚成一条仍行有界的记录,移动光标即断开生成新记录。② Quick Look 薄壳 `sync_lines` 仅按 `last_transaction` 的行区间 `splice` 镜像,不再每键 `to_vec()` 整 buffer(仅开文件那一次兜底)。守卫:`tn-editor` 新增 `continuous_typing_keeps_undo_records_line_bounded`(4000 行连打 500 字 + 换行,undo 栈合并为 1 条且 before/after 行数 < 8)+ 既有 `undo_history_does_not_store_full_buffer_for_single_line_edit`;`tn-ui` 既有 `edit_state_updates_line_mirror_without_replacing_whole_buffer`(`Rc::ptr_eq` 证镜像未被整 Vec 重建)。每键工作量结构上 O(1)(与 buffer 行数无关),仍需真机 4000 行连打肉眼确认无新增延迟。
- **目标**:解决当前 `Rc::make_mut`/整 buffer 快照导致的大文件输入风险。
- **读入**:TnE-06 后的 `Document` 和 undo 栈实现、`quick_look.rs` 4000 行限制/bench 入口。
- **改动范围**:把 undo/redo 从整 buffer 快照改为 transaction patch 或等价增量记录;保留用户可见撤销粒度。
- **验收**:4000 行文件连续输入无新增延迟;撤销/重做与 TnE-06 行为一致。
- **验证**:`cargo test -p tn-editor`;`cargo test --workspace --lib`;必要时加小型 perf 探针,不把 bench 变成必跑重负载。
- **禁止**:不换 renderer;不做 rope 大重写,除非先拆新包。

#### TnE-08 — `EditorElement` 只读几何/布局模型

- **状态(2026-06-08)**:✅ headless 已完成。新增 `crates/tn-ui/src/editor/` 模块骨架(`mod.rs` + `geometry.rs`),把现 Quick Look `uniform_list` renderer 内联的几何**复刻成纯函数 + 数据结构**,不依赖 GPUI/window、未接入任何 render 路径(`#![allow(dead_code)]` 标注脚手架)。提供:`Metrics{char_w,row_h=ROW_H(20),gutter=CODE_GUTTER(66)}`、`disp_width`/`prefix_cols`(CJK=2 列)、`max_cols`、`caret_x`(=gutter+prefix_cols×char_w)、`content_width`(最长行宽+1 列、`.max(viewport)`)、`max_h_offset`、`hover_char_at_x`(floor,拖选)/`caret_col_at_x`(就近边界,点击置位)、`visible_rows`/`row_out_of_view`(纵向可见窗 + caret-follow gate)、`follow_h_offset`(横向 caret-follow,4 列 margin)、`h_scroll_thumb`/`h_offset_from_drag`(横滚 thumb 几何与拖动逆运算互为反函数)、`caret_abs_x`(IME candidate 原点)。8 个 headless 单测覆盖 CJK 列宽、caret x、content 宽至少撑满视口、hover/caret 取整、可见窗、caret-follow、thumb↔drag 反函数。`cargo test -p tn-ui --lib` 132 测全绿、无警告。Quick Look 默认渲染未改、旧 `uniform_list` 未删(TnE-09 才据此实现 `Element` 绘制)。
- **目标**:先建立 renderer 所需的可测几何模型,不接入 UI 热路径。
- **读入**:`quick_look.rs:3231-3598` 现 renderer、`style.rs` token、`CLAUDE.md` uniform_list/CJK/滚动坑位。
- **改动范围**:新增 `crates/tn-ui/src/editor/` 模块骨架和只读 layout/prepaint 数据结构;计算行号槽、文本区、可见行、横纵滚几何、caret/selection rect 模型。
- **验收**:几何纯函数可测;不改变 Quick Look 默认渲染。
- **验证**:`cargo test -p tn-ui --lib`。
- **禁止**:不实现编辑输入;不删除旧 `uniform_list`。

#### TnE-09 — File 预览态只读 renderer 接入

- **状态(2026-06-08 续)**:◐ **paint + env 接线已落地,真机肉眼对照待 owner**。`quick_look.rs` 新增 `paint_file_preview`(自由函数)+ `QuickLook::file_element`(方法)+ `el_render`/`el_scroll_y` 字段:`TN_QL_ELEMENT=1` 时 File 只读预览改走自绘 `canvas`——用 `editor::{geometry,prepaint}` 模型自绘行号(右对齐)/ 语法着色文本 / 横滚 thumb,**按 1/2 列网格逐段 shape+paint**(ASCII 连排、CJK 单字 2 列步进)复刻固定单元格防漂移;纵向滚动 `el_scroll_y` 由滚轮驱动并按内容高 clamp,横向复用 `hscroll_px`,文本经 `with_content_mask` 裁到 gutter 右侧不侵入行号。**默认关 = 旧 `uniform_list` 路径(一键回退 = 不设 env)**。`cargo build/test -p tn-ui --lib` 通过(140 测、默认路径零改动)、fmt 干净、**一次编译通过**。真机待验:`TN_QL_ELEMENT=1 cargo run -p tn-app` 对照普通文本/CJK 长行/长文件的行号、着色、横纵滚动、滚轮方向;只读选区/复制/点击命中是 TnE-10(自绘路径暂无选区)。
- **状态(2026-06-08)**:◐ prepaint 模型就绪,GPUI paint + env 接线 + 真机对照待真机轮。本轮在 `editor/` 新增 `prepaint.rs`——只读 `EditorElement::prepaint` 所需的完整布局**纯函数**:`visible_row_indices`(按文档行数 clamp、底部 +1 行防缺口)、`row_top`、`gutter_label`(1-based 行号)、`content_origin_x`(=gutter−h_offset)、`prepaint_readonly` 返回 `ReadOnlyPrepaint{content_w,max_off,h_offset(clamp),rows,thumb(>8px 才显),content_x}`,忠实复刻现 File renderer 的 content 宽至少撑满视口、thumb `>8.0` 可见门、h_offset clamp。**已对照 gpui 0.2.2 `ShapedLine::paint(origin,line_height,window,cx)` 设计字段,paint 层只需 shape_line + 按这些 origin 绘制 + 画 thumb,是薄包装。** 5 个 headless 单测覆盖可见窗 clamp/pad、行号、短内容填满无 thumb、长行启用 thumb 且 offset clamp、≤8px 隐藏 thumb。`cargo test -p tn-ui --lib` 137 测全绿。**剩余(真机轮)**:实现 `Element`/canvas 的 `paint`(语法着色经 decoration_runs)、`TN_QL_ELEMENT=1` 门控接入 File tab(默认关、保持旧 `uniform_list`)、真机对照普通/CJK/长行/长文件 + 一键回旧路。理由:自绘渲染必须真机肉眼验(owner 一贯约束),headless 无法确认像素正确,故 paint 不盲写、只在真机轮落地。
- **目标**:File 预览态可通过 env gate 使用 `EditorElement` 绘制。
- **读入**:TnE-08 模块、`quick_look.rs` File tab render、横滚 thumb、预览打开/切换文件路径。
- **改动范围**:实现 GPUI `Element` 的只读绘制;保留 `TN_QL_ELEMENT=1` 或等价门控,默认可按 owner 决策保持旧路一版。
- **验收**:普通文本、CJK、长行、长文件只读预览与旧路一致或更好;失败可一键回旧路。
- **验证**:`cargo test -p tn-ui --lib`;真机对照 File 预览、横滚、长文件。
- **禁止**:不接编辑态;不做 Diff decoration。

#### TnE-10 — 只读选区/复制/CJK parity

- **状态(2026-06-08)**:◐ 选区/复制/CJK 命中已接入自绘路径,真机对照 + 下线旧路待。`file_element` 加 `on_mouse_down`/`on_mouse_move`/`on_mouse_up`:行号由 y 反推(`el_row_at`,含 `el_scroll_y`、clamp 到文档)、列由 `el_rel_x`(`pos_x − left − CODE_GUTTER + hscroll_px`)喂 `caret_col_at_x`(点击置位)/`hover_char_at_x`(拖选,`(row,hover)>=anchor?hover+1:hover` 含光标字符)、`pressed_button != Left` 兜底结束拖选;复用既有 `place_cursor`/`sel_range`/`copy`/`select_all`(预览态读 `file_data`),故 `Ctrl+C` 复制 / `Ctrl+A` 全选经 on_key 预览分支即生效。`paint_file_preview` 加 `sel` 参数:per-row `[ss,ee)` 字符段 → 显示列 x 区间画 `cola(accent,0.22)` 选区底色(under text、裁到 gutter 右侧),语义复刻 `edit_row_cached`(行内/跨行/末行)。**横滚条 thumb 可拖拽**:底部 14px 透明命中条(`hscroll_drag` + 已测的 `editor::geometry::h_scroll_thumb`/`h_offset_from_drag`),点 thumb 抓取偏移、点空轨道跳转居中、容器 move 优先处理 hscroll 拖动、`pressed_button`/up 兜底清除;无横向溢出时不 stop_propagation → 底行仍可选文本。`cargo test -p tn-ui --lib` 140 测全绿、fmt 干净、一次编译通过。**真机已验(2026-06-08,owner)**:点击置位 / 拖选 / 横滚后命中 / `Ctrl+C` / `Ctrl+A` 均 OK;**横滚条拖动**本轮补齐待复验。**旧 File `uniform_list` 路径暂保留为默认 fallback**(编辑态/Diff 仍用它);真机确认 parity 后再翻默认 + 下线旧 File 预览路径(本任务的收尾,不在本轮)。
- **目标**:让新只读 renderer 的命中、拖选、复制达到旧路径 parity,然后下线 File 预览旧路径。
- **读入**:`quick_look.rs` 鼠标 down/move/up、`pressed_button` 兜底、CJK 双宽命中、clipboard 逻辑。
- **改动范围**:File 预览态拖选、复制、全选、横滚偏移命中全部走新 renderer;旧 File 预览 `uniform_list` 路径删除或保留一版紧急 gate 后标注。
- **验收**:中文长行点击/拖选、横滚后命中、复制内容、全选与旧行为一致。
- **验证**:`cargo test -p tn-ui --lib`;真机 CJK 长行 + 横滚 + 拖选复制。
- **禁止**:不接编辑输入;不做软换行。

#### TnE-11 — 编辑态 transaction + caret/selection 绘制

- **状态(2026-06-08)**:◐ 自绘编辑态落地(含 TnE-10 收尾的默认翻转),真机待验。`file_element` 现同时承载预览 + 编辑:编辑态行源走 `edit` 镜像(`Rc<RefCell<Vec<String>>>` 直接借用、不每帧深拷)、画**瞬时反相块 caret**(`paint_file_preview` 加 `editing/caret/ime`:caret 单元格 `foreground` 底 + `chrome_bg` 字反相,EOL 也画块)+ 选区底色(复用)+ **IME preedit**(composing 串在 caret 处覆盖底色绘制 + accent 下划线)+ **`window.handle_input` 注册**(editing && !find_open,中文合成/WM_CHAR 进 `replace_text_in_range`,同旧路);新增 `el_follow_caret`(render 顶部、cursor 变才跟随的去抖,纵向 `el_scroll_y` + 横向 `follow_h_offset`)。输入 transaction 本就经 `Document`(TnE-06)。**TnE-10 收尾:`el_render` 默认开**(`new()` = `TN_QL_LEGACY` 未设),File 预览 + 编辑默认自绘;旧 `uniform_list` File 路径保留为 `TN_QL_LEGACY=1` 紧急回退(Diff 仍用它)。`cargo build --workspace` + `cargo test -p tn-ui --lib` 140 测全绿、fmt 干净。**真机待验**:连打 200 字符 cursor 不落后、中文 IME 合成显示、选区/拖选、查找跳转滚动、`Ctrl+S` 保存;有问题 `TN_QL_LEGACY=1` 回退。IME/preedit 与鼠标/滚动的完整 parity 收尾是 TnE-12。
- **目标**:编辑态切到 `EditorElement` 与 `tn-editor::Document` transaction,先保持瞬时反相块。
- **读入**:TnE-10 renderer、TnE-06 Document adapter、`quick_look.rs` on_key/on_mouse/edit state 切片。
- **改动范围**:单光标输入、选区替换、删除、移动、查找跳转、保存 dirty 状态全部通过 Document;renderer 绘制 caret/selection/search 命中。
- **验收**:快捷键心智不变;连续输入 200 字符真实 cursor 不落后文本。
- **验证**:`cargo test -p tn-editor`;`cargo test -p tn-ui --lib`;真机 200 字符连打。
- **禁止**:不加动画;不做多光标。

#### TnE-12 — IME/preedit/鼠标拖选/滚动跟随 parity

- **状态(2026-06-08)**:◐ 自绘**查找**三项真机回归已修(真机后续验)。真机报:自绘预览/编辑下查找①不跟随跳转 ②不高亮 ③无法输入中文。修法:① **查找跳转**——`find_next` 在 `el_render` 下走新增 `el_center_row`(把命中行滚到视口纵向居中)并 pin `last_follow_cursor`,使 render 顶的 `el_follow_caret` 不把它边缘弹走(旧路只调 `uniform_list` 的 `scroll_to_item`,自绘无效);② **全部命中高亮(突出显示)**——`file_element` 实时算 `all_matches(query)`,`paint_file_preview` 加 `matches` 参数,逐行画 `cola(ui.accent_alt,0.20)` 底色(与当前命中的选区 `accent 0.22` 异色区分);③ **查找中文输入**——根因:查找开时不注册 IME handler、且 `on_key` 对可打印键 `stop_propagation`(gpui 跳过 `translate_message`,IME 无法合成,同编辑器旧坑)。改:`find_key` 只接管命名/控制键(Esc/Enter/Tab/Backspace)返回 `handled`,**可打印键放行**给 IME handler;`handle_input` 改为 editing 即注册(不再 gate `!find_open`);`replace_text_in_range` 按 `find_open` 把提交/合成文本路由进 `find_query`/`replace_query`(`find_input`),查找条 active 字段回显 `ime_marked` preedit,编辑器正文在查找开时不重复画 preedit。`cargo test -p tn-ui --lib` 140 测全绿、fmt 干净。**真机待验**:微软拼音在查找框打中文 + 命中高亮 + Enter 跳转居中。剩余 parity(候选框 bounds 指向查找框、旧编辑 renderer 下线)仍属本包后续。
- **目标**:补齐编辑态最容易回归的输入法、鼠标和滚动细节,并下线旧编辑路径。
- **读入**:`CLAUDE.md` IME 两条坑、`quick_look.rs:3082` candidate 注释、`:3427-3534` 鼠标命中、`:3554-3598` 横滚。
- **改动范围**:IME preedit/candidate bounds、`VK_PROCESSKEY` 链路、拖选含光标字符语义、`pressed_button` 释放兜底、caret-follow 去抖全部在新 renderer 下跑通。
- **验收**:微软拼音中文输入、候选框位置、中文长行拖选、横滚命中不低于当前精度;旧编辑 renderer 下线。
- **验证**:`cargo test -p tn-ui --lib`;真机 `RUST_LOG=tn::ime=debug` + 微软拼音 + CJK 长行拖选。
- **禁止**:不引入帧泵;不把 IME 状态猜测写进核心 Document。

#### TnE-13 — Diff decoration 模型与只读 Diff renderer

- **状态(2026-06-08)**:◐ decoration + hunk-nav 模型就绪,只读 Diff renderer 接入 + 真机待。本轮在 `editor/diff.rs` 加**纯函数装饰模型**:`DiffRowKind{HunkHeader|Addition|Deletion|Context|Meta}`(+ `gutter()` 标记符、`is_content()`)、`classify_diff_line(raw)`(**`+++`/`---` 文件头先于 `+`/`-` 内容判定**,meta 含 `diff --git`/`index`/mode/rename/similarity/`Binary files`/`\ No newline`)、hunk 跳转 `hunk_header_rows`/`next_hunk`/`prev_hunk`(无环,调用方决定是否绕回;供 TnE-14 hunk 跳转)。8 个 headless 单测覆盖各行分类、+++/--- 不误判、gutter/is_content、hunk 前后跳转 + 空表。`cargo test -p tn-ui --lib` 140 测全绿。**剩余(真机轮)**:Diff tab 只读绘制走 `EditorElement`(复用 prepaint + 本装饰层 → 按 `DiffRowKind` 上色)、真机对照 agent 改动卡片打开 Diff、滚动性能。理由同 TnE-09:自绘渲染须真机肉眼验。**不做 accept/reject、不跑本机 git 处理远端**(TnE-13 禁止项)。
- **目标**:Diff tab 使用同一 renderer 的 decoration 层,不再维护另一套文本控件。
- **读入**:`parse_diff`、`ensure_diff`、Diff tab render、`gitutil::capture_bounded` 使用点、agent 改动卡片打开 Diff 的 workspace 路径。
- **改动范围**:在 `tn-editor` 或 `tn-ui::editor` 定义 diff line/decorations;Diff tab 只读绘制走 `EditorElement`。
- **验收**:Diff 展示、hunk header、增删行颜色、滚动性能与当前一致或更好。
- **验证**:`cargo test -p tn-ui --lib`;真机 agent 改动卡片打开 Diff。
- **禁止**:不在本包做 accept/reject;不跑本机 git 处理远端路径。

#### TnE-14 — Diff 拖选/复制/hunk 跳转

- **目标**:清掉当前「Diff tab 不支持拖选」待修项。
- **读入**:TnE-13 Diff renderer、File/Edit 选区复制实现、workspace 打开文件/Quick Look Diff target 路径。
- **改动范围**:Diff 中拖选、复制、全选、hunk 跳转到对应文件位置;hunk accept/reject 只留接口和禁用态说明。
- **验收**:Diff 可拖选复制;点击/命令可跳到对应文件;File/Edit/Diff 三态选择模型一致。
- **验证**:`cargo test -p tn-ui --lib`;真机 Diff 拖选复制 + 跳转文件。
- **禁止**:不实现远端 hunk apply;若 owner 要 accept/reject,另拆任务。

#### TnE-15 — `LineLayout` logical→visual headless 模型

- **状态(2026-06-08)**:✅ headless 已完成(提前于依赖 TnE-12 落地纯模型,不接 renderer)。`tn-editor` 新增 `line_layout.rs`:`WrapMode{None | Word{width_cols}}`、`VisualLine{logical_row,char_start,char_end}`、`LineLayout::build(lines,mode)`(贪心词换行:优先空格断行、无空格硬断、超宽单字独占一行且 `start` 严格前进防死循环、空行保一条空 visual)。映射/命中:`visual_count`/`visual_line`/`visual_range_of_row`、`logical_to_visual`(换行边界归下一 visual 行首、行尾留在末行)、`visual_to_logical`、`range_segments`(选区/查找 TextRange→每 visual 行 `(idx,col_start,col_end)` 局部列,跨 visual + 跨 logical 行)、`hit_test`(visual 行内 x→logical cursor,CJK=2 列、就近取整,复用扁平命中语义)。列=char 索引、宽=显示列(非 ASCII=2,tab=1 列同现 `disp_width`,tab-stop 展开不在范围)。9 个 headless 单测覆盖 NoWrap 1:1、空格断行、长 token 硬断、CJK 双列、空行/tab、换行边界映射、跨 visual/跨 logical 选区段、CJK hit-test、空文档安全。`cargo test --workspace --lib` 全绿(tn-editor 27)。**不接 GPUI renderer、不改代码文件默认横滚**(TnE-15 禁止项);软换行接入 File/Edit 渲染是 TnE-16。
- **目标**:软换行先在 headless 模型中成立。
- **读入**:`tn-editor::Document`、CJK 宽字符规则、查找/选区模型、愿景稿软换行策略。
- **改动范围**:`tn-editor` 增 `LineLayout`、`WrapMode`、logical line ↔ visual line 映射、visual hit-test 辅助;视觉行固定行高。
- **验收**:长 Markdown、CJK、tab/宽字符、跨 visual line 选区、查找命中映射都有单测。
- **验证**:`cargo test -p tn-editor`;`cargo test --workspace --lib`。
- **禁止**:不接 GPUI renderer;不改变代码文件默认横滚策略。

#### TnE-16 — 软换行接入 File/Edit

- **目标**:Markdown/txt/log/说明文档默认软换行,代码继续横滚。
- **读入**:TnE-15 `LineLayout`、TnE-12 renderer/命中路径、文件类型/语言识别位置。
- **改动范围**:File/Edit renderer 使用 visual line;选区、复制、查找跳转、滚动、横滚策略按 wrap mode 分流。
- **验收**:Markdown/txt/log 软换行不破坏滚动、选区、复制、查找;代码文件仍横滚;删除本文件「散文 / Markdown 长段落不能软换行」旧条。
- **验证**:`cargo test -p tn-editor`;`cargo test -p tn-ui --lib`;真机长 Markdown 段落跨视觉行选区。
- **禁止**:不做动画;不改 Diff accept/reject。

#### TnE-17 — `[editor]` 配置与 motion policy

- **状态(2026-06-08)**:✅ headless 已完成(schema + 降级策略模型,动画绘制是 TnE-18)。`tn-config` 新增 `[editor]` 段:`EditorAnimations{Off|Subtle|Full}`(`#[serde rename_all=lowercase]`,默认 `Subtle`)+ `Editor{animations}` + `EffectiveMotion{Instant|Subtle|Full}` + `Editor::effective_motion(reduced_motion, high_load)`——**系统减少动态效果 / 高渲染负载 / `off` 一律降级为 `Instant`**(光标瞬时反相块、与 TnE-12 基线一致),否则 `subtle`/`full` 透传。已导出至 `tn_config`;默认 `config/config.toml` 加 `[editor] animations` 段(带中文注释,`bundled_default_config_parses` 验证有效)。3 个单测覆盖默认 subtle、三值解析、off/reduced/load 降级矩阵。`cargo test --workspace --lib` 全绿(tn-config 42)。**不实现动画绘制、不改文本命中**(TnE-17 禁止项);UI 读取该 policy 驱动真实效果在 TnE-18(届时接 `perf` 高负载信号 + OS reduced-motion)。
- **目标**:先有可测试的动画开关和降级策略,再做视觉效果。
- **读入**:`tn-config` schema/默认配置、`crates/tn-ui/src/perf`、配置热加载路径、样式还原手册中 token 约束。
- **改动范围**:`tn-config` 增 `[editor] animations = "off" | "subtle" | "full"`;UI 读取 motion policy;reduced-motion/高负载降级条件建模。
- **验收**:默认 `subtle`;配置 `off` 时 renderer 能稳定得到“瞬时反相块”策略;无动画时行为与 TnE-12 一致。
- **验证**:`cargo test --workspace --lib`;必要时配置解析单测。
- **禁止**:不实现动画绘制;不改文本命中。

#### TnE-18 — subtle typing effects + perf gate

- **目标**:实现可关、可降级、不会影响真实 cursor 的轻量打字反馈。
- **读入**:TnE-17 motion policy、TnE-12 renderer caret、`perf`/`TN_PERF` 日志、CLAUDE.md 动画卡顿旧坑。
- **改动范围**:同一视觉行内 60-90ms caret 轻微 glide、块光标 squash/pop、新插入字符 70-110ms alpha/亮度 settle;所有 snap 场景立即归位。
- **验收**:关闭动画、reduced-motion、高负载降级时零行为差异;开 subtle 连续输入不卡,cursor 真实位置立即更新。
- **验证**:`cargo test -p tn-ui --lib`;真机 `TN_PERF` 对连续输入、IME、4000 行、大文件四场景。
- **禁止**:不使用 16ms 帧泵;不积累 per-char 队列;不改变文本宽度/行高/命中。

#### TnE-19 — 文档会话共享与升级入口

- **目标**:Quick Look 可以把当前文档会话升级给正式 Editor Pane,状态不丢。
- **读入**:`workspace.rs` pane/tab/layout 管理、Quick Look open/close 生命周期、`tn-editor::Document` 所有权模型。
- **改动范围**:新增文档会话 registry/handle;Quick Look 增「打开为编辑器」入口;handoff 后共享 buffer/undo/dirty/cursor。
- **验收**:浮层升级后内容、光标、undo、dirty 连续;Quick Look 仍是默认入口。
- **验证**:`cargo test -p tn-ui --lib`;真机 Quick Look 编辑几步 → 打开为编辑器 → undo/保存连续。
- **禁止**:不做多文件标签/LSP;不重构 workspace 分屏核心。

#### TnE-20 — Editor Pane UI/状态栏/生命周期

- **目标**:提供可长期编辑的 pane/tab 形态。
- **读入**:TnE-19 session handle、workspace pane rendering、状态栏模式、产品设计中的 panel/chrome 约束。
- **改动范围**:Editor Pane 宿主、状态栏(行列/编码/换行/dirty)、关闭 dirty guard、与 Quick Look 双向返回/切换规则。
- **验收**:Editor Pane 可编辑保存;关闭 dirty 守卫生效;状态栏准确;Quick Look 与 pane 之间不丢状态。
- **验证**:`cargo test -p tn-ui --lib`;真机浮层 → pane → 编辑保存 → 关闭/取消/放弃路径。
- **禁止**:不接 LSP/diagnostics/补全;不改变终端 pane 的 cwd/Explorer per-pane 语义。

#### TnE-21 — 全局回归与文档收尾

- **目标**:把完成的阶段从待修清单中清掉,并把项目记忆同步到正确源文档。
- **读入**:本节状态、`CLAUDE.md` 工作区/坑位、`docs/架构蓝图.md`、`docs/产品设计.md`、`docs/样式还原手册.md`、`docs/优化日志.md`。
- **改动范围**:更新状态表;删除已解决待修项;补架构边界、用户可见 UX、样式/验证要求;保留未做的 LSP、多光标、远端 hunk accept/reject 为后续项。
- **验收**:文档不再互相矛盾;每个已完成包有对应验证记录;未完成项仍在 `docs/未修复.md`。
- **验证**:`cargo test --workspace --lib`;`cargo build --workspace`;可行时 `$env:TN_AUTOQUIT="1"; cargo run -p tn-app`。
- **禁止**:不在收尾包偷偷实现新功能。

### 4. 产品形态验收参照

- **Quick Look 快编**(TnE-01 至 TnE-18 持续):默认入口。文件树 `Space`/`Enter`、agent 改动卡片、设置文件先开浮层;适合看文件、小改、配置、快速 Diff。
- **Diff Review**(TnE-13/TnE-14):agent 改动卡片进审查视图;短期 Diff 拖选/复制/跳转;hunk accept/reject 另拆任务,不混进 renderer 迁移。
- **Editor Pane**(TnE-19/TnE-20):长时间/多文件编辑时 Quick Look 升级为正式 pane/tab,共享同一编辑核心,有更稳定的空间/状态栏/后续 LSP 落点。
- **命名空间铁律**(全程不破):Host 直接读写;WSL 走 `\\wsl$` 映射;SSH/macOS 远端走 SFTP 远端 FS 后端,**不得把远端路径伪装成本机 `PathBuf`**。

### 5. 全局通过标准(Definition of Done)

- 打开普通文本文件即时可预览;进入编辑不改变原有快捷键心智。
- 连续输入 200 字符无明显卡顿,真实 cursor 不落后文本状态。
- 关闭动画 / reduced-motion / 高负载自动降级时,体验回到瞬时反相块且无行为差异。
- 中文长行点击、拖选、横滚、IME candidate 位置不低于当前精度。
- Markdown/txt/log 软换行不破坏滚动、选区、复制、查找;代码文件仍横滚。
- 4000 行文本滚动和编辑可用,无整面板每帧重建导致的 CPU 尖峰。
- 保存冲突、dirty close、外部修改检测有明确 UI,不静默覆盖或丢弃用户修改。
- File / Edit / Diff 三态交互无回归;`tn-editor` 核心逻辑有 headless 单测覆盖。
- 每个完成包都更新本节状态和对应源文档;上下文压缩前按 `AGENTS.md` 记录目标、关键改动、验证结果和未完成事项。

## 对面板进行解耦:per-pane 工作区上下文 ✅(2026-06,SFTP 首版已补)

**目标(需求原文)**:让每个终端窗格拥有自己的「工作区上下文」,而不是让左侧文件树、Agent 改动列表、用量统计都依赖一个全局状态。

### 已完成(本轮做了什么)

- **每个 pane 绑定自己的文件树状态** ✅:`ExplorerView` 仍是单一渲染实例,但 Workspace 按 `PaneId` 存 `explorer_states`(`ExplorerSnapshot` = 展开态 + 选中文件)+ `explorer_pane`(记当前展示的 pane)。render 焦点跟随**区分两种事件**:焦点**换 pane** → `ExplorerView::switch_pane`(保存旧 pane 快照 / 恢复新 pane 快照,各 pane 文件树互不串台);**同一 pane 内 `cd`**(OSC 7)→ 沿用 `follow_root`(保留子目录展开态)。切回某 pane 即恢复它自己的 root / 展开态 / 选中文件。快照保存时惰性 `retain` 裁掉已关闭 pane(免逐 `panes.remove` 钩子);恢复经纯函数 `snapshot_under_root` 过滤到新 root 内(越界展开/选中丢弃,headless 单测覆盖)。
- **Agent 信息跟 pane 绑定** ✅(本就如此,本轮只验不回归):agent 身份 / 用量环 / 「本次改动」/ git diff watcher / 会话态全在 `TerminalView`(per-view),状态栏只读**焦点 pane** 的用量。不会被其它 pane 的 cwd 或新启动会话串台。
- **「打开文件夹」收敛到当前焦点 pane** ✅:`cd_panes_to_root`(无差别广播)→ `cd_pane_to_root(id, …)`(单 pane)。`menu_open_folder` 进入前捕获焦点 pane,只对它 `cd` + `set_rail_root`,其它 pane 保持各自目录;agent pane **永不被 `cd`**。Host pane 选 Windows 路径、WSL pane 经原生 picker 浏览 `\\wsl$\<distro>\`(`from_accessible_path` 识别 UNC)后映射回 Linux 路径 `cd`。
- **命名空间边界不混淆** ✅:Windows `PathBuf` / WSL Linux 路径 / SSH 远端路径经 `ExplorerRoot::path_for_namespace` 显式转换;`is_host_process_path` 拦截不兼容映射(WSL UNC 不会塞给 cmd/pwsh)。

### 远端 FS 后续增强(2026-06-07 续:hunk 可视按钮 + 刷新 + 失败提示已落地)

- **远端 hunk accept/reject 可视按钮** ✅:Quick Look 的 **Diff tab** 在每个 `@@` 行右侧渲染「接受 / 拒绝」按钮(仅远端 diff,`remote_diff_file` 存在时;由 `is_remote_diff` gate)。点击 → `QuickLook::apply_hunk(hunk_index, action)`:后台线程**重新拉取**当前 `git diff`(渲染用的 `DiffLine` 已丢失补丁所需的原始 hunk 正文)→ `remote_git::parse_file_diff` → `apply_remote_hunk` 经 SSH 跑 `git apply --cached -`(接受)/ `git apply --reverse -`(拒绝),patch 走 stdin。`DiffLine` 加 `hunk_index`,`parse_diff` 与 `remote_git::parse_file_diff` **同序计数 `@@`**,故点击行能映射回正确 hunk(单测 `parse_diff_numbers_hunks_in_lockstep_with_remote_file_diff` 锁死一致)。
- **远端 git 刷新策略** ✅:apply 成功 → `diff_dirty=true` + `ensure_diff` 重新拉 diff(已应用/撤销的 hunk 自动消失)+ `cx.emit(QuickLookEvent::RemoteChangesDirty)`;workspace 收到后刷新每个 pane 的「本次改动」(远端 pane 经 `changes_for_remote`)并 `explorer.mark_stale()`——和本地保存同一条刷新路径(远端 FS 编辑文件监听看不到,必须显式刷)。
- **失败提示** ✅:apply 期间 `hunk_busy=true` 禁用按钮(防双击发两条冲突补丁、按钮转灰);失败 → `hunk_error` 红色横幅(复用 save_error 范式,「应用失败:<stderr>」+ 关闭按钮);开新文件/切文件即清。
- **仍未做(真机)** 🧭:**真实 SSH/SFTP 端到端验证**(hunk 按钮点一下真的改远端 working tree、刷新观感、并发/超时/`git apply` 拒绝补丁的失败文案)——逻辑有 headless 单测(`remote_git` 全套 + `parse_diff` 一致性),但需真机肉眼。按钮目前**水平随长行滚动**(hunk 头通常短,够用);若远端 diff 行极长需要常驻浮起按钮,再做覆盖层。

### 如何验证

- **自动**:`cargo test -p tn-ui --lib`(含 `snapshot_restore_filters_to_new_root` / `snapshot_restore_drops_out_of_root_selection` 两个纯函数单测 + 四道守卫全绿);`cargo build --workspace`;`$env:TN_AUTOQUIT="1"; cargo run -p tn-app`(网格收敛、exit 0)。
- **真机肉眼**(`cargo run -p tn-app`,改完先关旧 `tn.exe` 防重链锁):分屏两 pane 各 `cd` 到不同目录、各展开不同子树、各选中不同文件 → 来回切焦点,文件树**各自恢复**;同 pane 内 `cd` 进子目录展开态保留;「打开文件夹」只当前焦点 pane `cd` 过去、其它 pane 与 agent pane 不动;焦点 SSH pane 点「打开文件夹」→ 应用内远端目录 picker,确认后只该 pane 远端 `cd`;远端文本 Quick Look 编辑保存遇外部变化时必须弹冲突操作条;agent pane 用量环 / 「本次改动」切到别的 pane 不串台。

逐项明细见 [CHANGELOG.md](../CHANGELOG.md) 面板解耦段;架构「做了什么 / 为什么」见 [架构蓝图.md](架构蓝图.md) §8 原型同步轨道 ⑤;UX 见 [产品设计.md](产品设计.md) §6.3;操作指南见 [CLAUDE.md](../CLAUDE.md) 现状段「面板解耦」。

## 对每一页面的样式重新设计，增加更多细节，尝试使用更高级的界面库或者自研界面库


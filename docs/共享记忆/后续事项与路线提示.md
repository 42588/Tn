# 后续事项与路线提示

## 定位

本文承接原 `CLAUDE.md` 中近期 UI 状态、per-pane 工作区提示，以及未做/后续打磨项。入口只保留短索引。

## 背景

- 来源：`CLAUDE.md` 原 UI 状态提示与 `## 未做 / 后续(打磨项)`。
- 职责：记录仍需主控或后续任务跟进的路线提示，不替代 `TODO.md` 和 `docs/任务/` 的任务流水。

## 具体内容

### UI 现状与 per-pane 工作区提示

> **现状**:全 UI 已逐组件对齐 mockup、主题色硬编码清零、四道守卫把关;`design/panels/` 新原型的主界面已 1:1 端口(agent 活动栏「本次改动」接真实 `git diff HEAD`、欢迎 launchpad + 应用内 Agent 编辑器、Quick Look 速览浮层 + 自绘编辑器、app 菜单 popup、分屏启动器、布局管理器)。「做了什么 / 为什么」见 [系统架构索引 §8](../系统架构索引.md) + [产品体验索引 §6.4](../产品体验索引.md),逐项明细见 [CHANGELOG.md](../../CHANGELOG.md);余欢迎页最近目录待接。**窗口保持 Opaque**(试过默认 acrylic 透背被 owner 否,见坑)。
> **面板解耦(per-pane 工作区)**:每个分屏 pane 自带文件树状态(`ExplorerSnapshot`;Workspace 按 `PaneId` 存 `explorer_states` / `explorer_pane`)。render 焦点跟随**分两路**——**换 pane** → `switch_pane`(存/取快照),**同 pane `cd`** → `follow_root`(保子目录展开态),**别混成一个**(那样跨 pane 切换会揉掉展开态)。「打开文件夹」`cd_pane_to_root` 只动焦点 pane;agent 用量 /「本次改动」/ git watcher 早已 per-pane(在 `TerminalView`)。**欢迎页焦点「pane」= `PaneId::MAX` 占位 dummy**(无 `LaunchSpec`,防与真实 pane 撞 + 关页误删 pane 0 致 abort;故「打开文件夹」对无 spec 用原生选择器,见坑)。

### 未做 / 后续(打磨项)

- **M2 SSH**(✅ 首次真机验证 2026-06-05:密钥 + 交互 shell 登录 AlmaLinux 9.7/WSL)。**SSH UX 优化路线** = 三轴 **A 交互 / B 界面 / C 提示**,**A/B/C 全轴已落地**;整体设计稿 = [`design/panels/06-ssh.html`](../../design/panels/06-ssh.html),**逐项实现明细见 [新增模块索引.md](../新增模块索引.md)「SSH 连接 UX 优化路线」**(此处只列状态 + 计划,细节不复制)。
  - **✅ A/B/C 全轴已落地**(连接卡共用 `terminal_view` 覆盖范式,优先级 指纹 > 密码 > 错误 > 进度;按钮经 `Ssh{Retry,Close}Requested` / `SshRememberPassword` 回 workspace):**A** 最近 + ⭐收藏(`ssh_recents.json`)+ ssh-config 别名枚举合并去重· **B** 进度卡 / 指纹 TOFU(`NeedHostKeyConfirm`)/ 密码框 / 断线重连横幅 + 连接态胶囊· **C** 可操作错误卡 / 拨前校验(`validate_ssh_target`)/ 首连引导。**逐项明细在 [新增模块索引.md](../新增模块索引.md),此处不复制。**
  - **🧭 后续计划(打磨,低优先)**:B2「仍信任并更新」(改 known_hosts 旧条目)· B4 倒计时 + 立即重连(需后端 reconnect-now 通道)· C1 超时卡 / 改用密码 / 编辑连接 · tab 上的连接态点 · 记住密码升级凭据管理器(替代内存)· `~/.ssh/config` 进阶(ProxyJump)。
  - **真机仍未走过**:连接卡 B1/B2/B3/C1/B4 肉眼 · A4 ssh-config 行实景 · C2/C3 提示 · keyboard-interactive(测试机未开放)· 断线重连实景 · ssh-agent。
- **Quick Look 编辑器**:已含 选区 / 拖选 / 复制粘贴 / 撤销重做 / 查找替换 / 反相块光标 / 固定单元格 / 横向滚动 / 预览态只读选区(详见坑 + CHANGELOG)。本地文本保存已接 `TextFormat` + `FileGuard`:打开记录编码/换行/mtime/size/hash,`Ctrl+S` 前发现磁盘外部修改则出「重新载入 / 取消 / 覆盖保存」,正常写回保留 UTF-8 BOM / UTF-16 / GBK 与 LF/CRLF。更远期(若需要):多光标、列选、**Diff tab 拖选**(本轮只做 File/编辑)、增量语法解析、LSP/补全——已超出「速览快编」定位,重度编辑回终端用 vim。
- **命令面板中文搜索**(parked):面板靠 `on_key` 读 ASCII `key_char`,而 IME 合成的中文**只经 `EntityInputHandler` 投递**(同终端「打中文」根治),面板还没接 → 目前**只能搜英文**。补法:给 `Workspace` 接 `EntityInputHandler`(`palette_marked` 合成态 + 提交进 `palette_query`)+ 面板内挂 canvas 注册 `handle_input` + `on_palette_key` 对可打印键**放行**走 IME 提交链 + 渲染预编辑串。
- **分屏交互**:✅ 分隔线鼠标拖拽(**commit-on-release**:拖动只移动一条 2px 预览线、释放才改权重 resize 一次——避免 ConPTY 每帧 resize 抖动;配合 `resize_conpty` 顶锚定长高,**拖大窗格不再丢滚动历史**;把手平时隐形、hover 微亮)· 🧭 拖拽停靠(drag-dock:拖到边=分屏、拖到中=标签组)。
- **自定义 `TerminalElement`**(M1.2b):GPUI `Element`(layout/prepaint/paint)+ 字形图集 + typed-quad 批处理 + 光标/选区绘制
  (见 外部参考资料索引 §2;GPUI 自管图集/DirectWrite,**不写裸 D3D**)。当前 div + run 批处理即现版本。
- **真机肉眼项**:颜值微调 · 标题栏拖动/控制点验 · 光标闪烁/连续动画(需帧时钟;agent 思考态 PTY 不可观测,不伪造)·
  per-pane cwd 经 OSC 7 实时跟随 · Quick Terminal 动画顺滑/多显示器·高 DPI 定位/首帧不空白。
- **其他后置**:主题/配色导入(iTerm/WT/base16)· 窗口 `[appearance].opacity/backdrop`、`[font].fallback`(已解析未应用)·
  OSC 8 超链接 · kitty 键盘协议 / DECKPAM 小键盘 / win32-input-mode · 历史 block 的逐行覆盖 chrome。

## 验证 / 状态

- 2026-06-09：文档重构迁移，未运行编译。

## 反向链接

- 母文档：[CLAUDE.md](../../CLAUDE.md)
- 相关索引：[产品体验索引](../产品体验索引.md)、[新增模块索引](../新增模块索引.md)
- 相关任务：大型文档拆分任务由主控统一记录。

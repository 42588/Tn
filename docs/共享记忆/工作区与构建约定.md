# 工作区与构建约定

## 定位

本文承接原 `CLAUDE.md` 中工作区 crates、依赖边界、构建运行和测试命令的长期知识。入口只保留链接，细节在本文维护。

## 背景

- 来源：`CLAUDE.md` 原 `## 工作区(crates)` 与 `## 构建 / 运行 / 测试`。
- 职责：记录 crate 边界、headless 约束、常用命令、性能构建约定和外部参考源码位置。

## 具体内容

### 工作区(crates)

```
tn-core    alacritty 包装:Term + VTE 解析 + TerminalSnapshot(每格 fg/bg + cursor)+ Palette/RGB
           (ANSI16 + 256 + OSC + INVERSE)+ CellRun/row_runs(run 批处理)+ InputMode + 滚动 + 选区 + 子串搜索(search→SearchMatch,跨历史)+ URL 检测(snapshot.urls→UrlSpan)+ generation(渲染缓存代际)+ resize/resize_conpty(后者顶锚定长高,防 ConPTY repaint 吃滚动历史,见坑)。
tn-pty     PtyBackend trait + LocalPty(ConPTY,Drop 杀子进程)+ wsl.rs(parse_distros 解码
           `wsl --list --quiet` 的 UTF-16LE + list_distros)+ ssh.rs(SshBackend 实现 PtyBackend——
           专属 tokio 线程把 russh 的 async channel 桥成同步 Read/Write;认证探测 + 密钥/密码/keyboard-interactive;
           **结构化连接事件** `PtyEvent::{SshProgress(phase),SshFailed(kind,offered),NeedPassword(error),NeedHostKeyConfirm(fingerprint),Connected(method),Disconnected}` 驱动 UI 进度/错误/密码/指纹卡 + 连接态;host-key TOFU 经 `HostKeyVerdict` 回传;`list_ssh_config_hosts`/`parse_host_aliases` 枚举 `~/.ssh/config` 的 `Host` 别名 = 连接器第三段 A4)。WSL 复用 LocalPty 跑 wsl.exe。
tn-config  配置 + 主题(TOML schema、%APPDATA%\Tn、首次写默认、热重载源)+ quick_terminal.rs
           ([quick_terminal] schema + 滑入几何 shown/hidden/frame_rect + ease_out_cubic + 热键解析 parse_hotkey)
           + **`[editor]`(TnE-17)**:`EditorAnimations{Off|Subtle|Full}`(默认 subtle)+ `Editor::effective_motion(reduced_motion,high_load)`→`EffectiveMotion{Instant|Subtle|Full}`(off/减少动态/高负载降级为瞬时=TnE-12 基线;动画绘制是 TnE-18)
           + append_profile/profiles_toml_fragment(把单个 `[[profiles]]` 片段**追加**写回 config.toml、不丢注释;当前 SSH 连接器不再提供「保存为连接」入口,保留为兼容/手编配置工具,见坑)
           + **append_agent/remove_agent + agents_toml_fragment**(应用内 Agent 编辑器用:`[[agents]]` 块级追加/删除,泛化的 `block_ranges` 保注释)+ **ACCENT_SWATCHES**(agent 编辑器颜色预设,放 config 域避开 tn-ui 硬编码守卫)。`AgentManifest.sidecar`(stdio/JSONL 遥测 sidecar 命令)+ `runtime_support`/`allow_network`(非 PTY 运行时 + 网络默认拒绝)。
tn-shell   shell 集成:旁路 vte::Parser 把 OSC 133/633/7 解析成 BlockEvent + pwsh 集成脚本/nonce。
tn-blocks  Warp 式 block 状态机:BlockEvent(+行/时间)→ Block(命令/输出区间/退出码/时长)。
tn-editor  **headless 编辑核心(P1 首版)**:Quick Look 旧 `op_*` 文本编辑纯函数已迁移到这里,当前提供
           `Cursor`/`TextRange`/`Document`/`Selection`/`CursorSet`(单主光标)/`EditTransaction`/`UndoStack`/`SearchState`,
           以及 `char_to_byte`/`line_chars`/insert/newline/backspace/delete/move/page/range/multiline 等
           Vec<String> + char-column 契约;无 gpui 依赖。Quick Look 编辑态已通过 `QuickLookEditState` 薄壳接入
           `Document` 主状态,旧 `uniform_list` renderer 仍读适配后的 `Rc<Vec<String>>` 快照。**`line_layout`(TnE-15)**:headless 软换行模型 `WrapMode{None|Word{width_cols}}` + `LineLayout`(logical↔visual 映射、`range_segments` 选区/查找投影、`hit_test` visual 命中,CJK=2 列),供 Quick Look、Editor Pane、Diff Review 未来共享(TnE-16 才接 renderer)。
tn-agent   **Agent Host 平台(对具体 agent 零知识)**:AgentId(开放字符串身份,替代闭合 AgentKind 枚举)·
           AgentDescriptor + AgentCapabilities + AgentRuntimeKind(身份/能力插槽/运行位置)· AgentEvent + AgentStatus(UI 唯一输入契约)·
           AgentAdapter trait(私有格式/实时通道→统一 AgentEvent;默认实现 = 无遥测)+ GenericAdapter(有身份无遥测,供配置级 agent)·
           ExternalEventAdapter/ExternalProcessAdapter(JSONL/stdout 外部事件队列;`from_descriptor` 据 `realtime_command` 拉起 sidecar 子进程)+ AgentRegistry(按 id/命令解析,**空 = 纯 shell 宿主**;register_manifest 把 config `[[agents]]` 注册为 generic,已注册 id 则 no-op[内置 telemetry adapter 优先])· AiUsage + pricing(通用用量契约,从 tn-ai 上移)。
           **`AgentDescriptor::sidecar_launch()` = 默认拒绝网络的单点决策**(`SidecarLaunch::{None,SpawnNow,Confirm}`:本地 sidecar 直起 / networked+allow_network 才弹确认 / 否则不起)。
           **边界:AgentRuntimeKind(进程/协议位置)≠ FileNamespace(文件 I/O 命名空间)**;`AgentEvent::CwdChanged` 只喂目标 pane 上下文、不碰全局 Explorer。
tn-ai      **内置 Claude/Codex AgentAdapter(平台的两个种子 provider,可移除)**:claude.rs / codex.rs 解析本地会话 JSONL
           → token/上下文/估算花费(adapter 薄包装这些纯解析器);detect.rs `resolve_pane_session`(launch 后 stale→fresh,
           **吃 `&dyn AgentAdapter` 泛化**,第三个 agent 无需新 match)+ `*_is_subscription`。`builtin_registry()` 建 Claude+Codex,
           **但默认 app 不再自动注册它**(Agent Host 出厂无内置 agent);保留作种子/测试/用户重新注册 telemetry 的入口。**`builtin_adapter_for_manifest`**:config `[[agents]]` 命令命中 claude/codex → 返回内置 adapter(`ClaudeAdapter::with_descriptor` 用**用户的颜色/名称** + 内置解析、`usage` 开)→ 用户加个 `claude` agent **自动有真实用量环、无需 sidecar**。**AgentKind 闭合枚举已全删。**
tn-ui      GPUI 前端(唯一链接 gpui 的库):style(共享 Calm Glass 令牌 + col/cola/soft_shadow/shadowed/icon,
           单一真源)· assets(内嵌 SVG 图标 + 动态用量环)· input(按键编码)· gitutil(共享**有界** git 调用 + numstat/preview 解析,quick_look diff 与活动栏「本次改动」共用)·
           terminal_view/(文件夹模块:mod 渲染核心[正文落 `.body` 11/15 内边距:`BODY_PAD` 偏移网格/光标/鼠标命中/适配]+ `sync_shell_agent`[敲 claude/codex 切 agent 态]+ `refresh_changes`/`spawn_change_watcher`[notify 文件监听 → git 刷新]+ io 后台线程[用量轮询 + 重绘泵 + repaint 里跑 `sync_shell_agent`]+ header agent 头/活动栏[`rail_files` 真实 `git diff HEAD`、诚实状态、点卡片发 `OpenInQuickLook`]+ launch LaunchSpec;**`EntityInputHandler` 接 IME→中文写 PTY**;**SSH 连接卡覆盖层**[`ssh_progress`=B1 进度卡 / `ssh_error`=C1 错误卡 / `ssh_password_prompt`=B3 密码框 / `ssh_hostkey`=B2 指纹信任卡 / `ssh_conn`=B4 连接态(shell 头连接态信息胶囊 + 断线重连横幅),优先级 指纹>密码>错误>进度;按钮发 `SshRetryRequested`/`SshCloseRequested` 给 workspace]+ **per-pane sidecar 遥测**[launched agent 据 `sidecar_launch()` 拉起自己的 `ExternalProcessAdapter`(`realtime_adapter` 持有、drop 杀子进程)+ 事件 poller;networked sidecar 弹 `sidecar_confirm` 网络确认卡(允许并连接/拒绝)];可见光标 + generation 渲染缓存)· perf(PerfStats,TN_PERF 渲染日志)· block_view(block 卡片)· editor(`EditorElement` 只读几何/布局**纯函数**骨架:`geometry`(TnE-08:`Metrics`/`disp_width`/`caret_x`/`content_width`/`hover·caret_col_at_x`/`visible_rows`/`follow_h_offset`/`h_scroll_thumb`↔`h_offset_from_drag`)+ `prepaint`(TnE-09:`prepaint_readonly`→`ReadOnlyPrepaint`、`visible_row_indices`/`row_top`/`gutter_label`/`content_origin_x`,只读 prepaint 全量,字段对齐 gpui `ShapedLine::paint`)+ `diff`(TnE-13:`DiffRowKind`/`classify_diff_line`[+++/--- 文件头先于增删]/`hunk_header_rows`·`next_hunk`·`prev_hunk` hunk 跳转);复刻 Quick Look 内联几何;**File 预览 + 编辑器现默认走自绘**(`quick_look::paint_file_preview`/`file_element`/`el_follow_caret`,`el_render` 默认开;`TN_QL_LEGACY=1` 紧急回退旧 `uniform_list`,Diff 仍用旧路):选区/复制/CJK 命中/横纵滚动/滚动条拖动 + 编辑态瞬时反相块 caret/IME preedit/handle_input/caret-follow 都已接入,真机持续验收中)·
           explorer(文件树侧栏 + 键盘 nav:`↑↓` 移选中 / `Space`·`Enter` 开 Quick Look / focus-on-click;`select_adjacent_file` 供浮层换文件)· welcome(欢迎 launchpad:新标签/首标签的启动磁贴 + 快捷键,点磁贴启动会话;**「+ 添加 Agent」磁贴**`AddAgentRequested` + 自定义 agent 磁贴 hover **✎/✕**`Edit/DeleteAgentRequested`[`is_managed_agent`=有 config `[[agents]]` manifest 的 agent profile],事件回 workspace 开/删 agent 编辑器)· ssh_recents(SSH 最近/收藏连接持久化:`SshRecent`/`SshRecents`,成功连接 upsert + ⭐收藏 + 中文昵称 name + 相对时间,JSON `%APPDATA%\Tn\ssh_recents.json`;连接器长期保留的数据源,A1/A2)+ `format_target` 共享连接目标显示· quick_look(原 viewer;文件/Diff 速览浮层:贴树右缘、浮于终端、不占分屏,`quicklook_fill` 烤实暗玻璃 + `quicklook_frame` 冷能量边 + 左缘 seam + 虚拟化代码区;预览导航 + **自绘小编辑器**[编辑态主状态 = `tn-editor::Document` 经 `QuickLookEditState` 薄壳,旧 renderer 读 `Rc<Vec<String>>` 快照;预览态只读选区仍在 Quick Look 外层;选区/复制粘贴/撤销重做/全选/鼠标点位/编辑态高亮/查找替换由 Document 驱动,`Ctrl+S` 写盘];**diff 惰性**[`diff_dirty`,只在切 Diff tab 时 `ensure_diff` 跑 git]+ **有界 git** 走 `crate::gitutil::capture_bounded`[丢线程 + 超时 + `CREATE_NO_WINDOW`,防 git 卡死 UI,见坑];`QuickLookEvent` Nav/Close 回 workspace)· workspace(标题栏 + 标签/n-ary 分屏 + 侧栏 +
           状态栏 + 命令面板 + SSH 连接器[`render_ssh_prompt`/`ssh_conn_rows`:最近/收藏(json)+ ssh-config 别名(A4)合并去重、⭐收藏/取消收藏承担长期保留(A2)、收藏行 pen 重命名(`SshRenameDraft` + `EntityInputHandler` 支持中文)、面板 `max_w/max_h` 防撑爆、`validate_ssh_target` 拨号前校验红 chip = C2、空态三步引导 = C3]+ 新会话分屏启动器[`render_split_launcher`]+ app 菜单 popup[`render_app_menu`]+ 布局管理器[`render_layout_manager`,7 槽存/取/删]+ **Agent 编辑器**[`render_agent_form`/`on_agent_form_key`:应用内加/改/删 agent 的居中玻璃浮层,收集 名称/命令/颜色(`ACCENT_SWATCHES`)/Ink 自绘光标 + **高级 · 遥测 sidecar**(`AgentField::Sidecar` + 「联网」开关 → manifest `sidecar`/`runtime_support`/`allow_network`,GUI 即可配真实遥测,不必手编 config) → `save_agent_form` 写 `[[agents]]`+`[[profiles]]` → `reload_agents` 重建注册表 global+launch_profiles+welcome(`subscribe_welcome` 复用订阅)即时生效;名称字段中文经 `EntityInputHandler` 多路复用(与 SSH 重命名同源,`agent_form_field==Name` 时 IME 开),命令字段 ASCII;`slugify` 派生稳定 `AgentId`、命令首词进 aliases]+ Calm Glass chrome)· layout(布局序列化:`LayoutNode`/`LayoutPane`/`Layouts`,JSON 持久化,加载按结构重拉起)· platform(Windows-only:全局热键 + 置顶/滑动 SetWindowPos)·
           quick_terminal(无边框置顶 PopUp 窗口 + 启动器)。
tn-app     二进制 `tn`:开窗 + 接线 + 崩溃保护 + 文件日志。
tn-cli     headless ConPTY 烟雾测试工具(可 `-- <program> [args]` 测任意子进程;`TN_RESIZE_EXP=1|locked|topgrow|interactive|prompt`
           = ConPTY resize 探针:naive 实测增高吃滚动历史、locked 验证行锁定(已撤销)、**topgrow 验证 resize_conpty 顶锚定长高零丢失**)+ `tests/conpty_pipeline.rs` 全链路集成测试。
```

依赖方向(无环):`tn-blocks → tn-shell → tn-core`;`tn-editor` 独立 headless;`tn-config ← tn-agent ← tn-ai`(平台 ← 内置 adapter);`tn-ui → 全部 headless crate`。
**铁律**:`gpui` 只能出现在 `tn-ui`/`tn-app`;其余 crate 必须能 **headless** 编译与测试。别把 alacritty 的类型
泄漏出 `tn-core`(如 `CellRun` 暴露 bold/italic 布尔而非 `Flags`);`tn-shell` 用 `vte` crate 直连(不经 alacritty)。

### 构建 / 运行 / 测试

`cargo` 在 `%USERPROFILE%\.cargo\bin\cargo.exe`(不在 bash PATH 上——PowerShell 里用全路径,或新开 shell)。

```powershell
cargo build --workspace
cargo test  --workspace --lib                  # 全 crate 库单测(headless)+ `--test conpty_pipeline` 1 集成
#   注:`--lib` 只跑库单测;去掉 `--lib` 还会跑集成测试(`conpty_pipeline`,需起 ConPTY)。设计复刻 example 已移除(原型改对照 design/panels)
$env:TN_PERF="1"; cargo run -p tn-app          # 开渲染性能日志(tn::perf,~1/s:渲染率/缓存命中率/重建耗时)
cargo bench -p tn-core --bench engine          # criterion 热路径基准:advance(VT 解析)+ snapshot/row_runs(每帧渲染数据)
cargo run   -p tn-cli                          # ConPTY 烟雾测试:起 shell、把网格渲染到 stdout、PASS/FAIL
cargo run   -p tn-cli -- wsl.exe -d Ubuntu -- echo HELLO_TN_MARKER   # 验证 WSL 经 ConPTY 跑通
cargo run   -p tn-app                          # 开终端窗口
$env:TN_AUTOQUIT="1"; cargo run -p tn-app      # headless 自测:首个 pane 跑命令、dump 网格、退出(exit 0)
$env:TN_DEMO="1";     cargo run -p tn-app      # 演示:窗口里自动步进滚动/选区/分屏/改尺寸,然后退出
```

> **dev 构建的运行性能(开发期跑高频重绘 TUI 必读)**:根 `Cargo.toml` 用 `[profile.dev] opt-level=1` + `[profile.dev.package."*"] opt-level=3`(Bevy「optimize deps, debug your code」配方)。默认 dev 全 opt0,而 alacritty VT `advance` / tn-core `snapshot`·`row_runs` / gpui 布局·paint 是 Claude Code(Ink,每秒数十次整屏重绘)的热路径 → opt0 下慢几十倍,直接表现为输入卡顿 + 面板拉起慢,且把零分配/channel/增量解析等逻辑优化的收益**完全淹没**。代价:首次 `cargo build` 重编依赖一次,之后改 tn-ui/tn-app 增量照常快。**(2026-06 复盘「做了很多优化依旧卡」的决定性真因。)**

参考源码(仓库外浅克隆,供设计研读):`d:\coder\_refs\terminal`(Windows Terminal)、`d:\coder\_refs\ghostty`。

## 验证 / 状态

- 2026-06-09：文档重构迁移，未运行编译。

## 反向链接

- 母文档：[CLAUDE.md](../../CLAUDE.md)
- 相关索引：[系统架构索引](../系统架构索引.md)
- 相关任务：大型文档拆分任务由主控统一记录。

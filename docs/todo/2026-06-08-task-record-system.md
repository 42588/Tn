# 构建 TODO 任务记录体系

## 状态

已完成。

## 目标

建立根目录 `TODO.md` 作为任务母索引,并用 `docs/todo/` 子文档承载每次任务的完整记录,避免任务历史继续堆成单个大型流水账。

## 范围

- 新建 `TODO.md`。
- 新建 `docs/todo/` 任务详情目录。
- 补充当前任务、后续大文档拆分任务、近期 agent surface 修复任务的初始记录。
- 在 `AGENTS.md` 和 `CLAUDE.md` 中强调新增信息与任务记录的落点规则。

## 执行清单

- [x] 确认根目录原先没有 `TODO.md`。
- [x] 创建 `docs/todo/` 目录。
- [x] 创建 `TODO.md` 母索引。
- [x] 创建当前任务详情页。
- [x] 创建大型文档拆分待办详情页。
- [x] 创建 agent surface 修复完成记录。
- [x] 更新 `AGENTS.md` 的入口级规则。
- [x] 更新 `CLAUDE.md` 的主记忆规则。
- [x] 核对新增链接和状态。

## 验证

已用 `rg` 和 `git diff` 做文档落盘核对。该任务只改文档,不需要编译测试。

## 后续

后续每次项目任务按 `TODO.md` 母索引 + `docs/todo/` 子文档的结构更新。

# 新增模块与功能记录

## SSH 远程连接支持 (M2b)

### 1. 已实现功能
- **`~/.ssh/config` 解析支持**：使用 `ssh2-config` crate 读取本地 SSH 配置文件，自动提取指定主机的 `HostName`、`Port`、`User`、`IdentityFile` 等配置，并与用户的 Tn 配置相融合。
- **TOFU (Trust On First Use) 与公钥安全验证**：通过 `russh::keys::check_known_hosts_path` 验证公钥。首次连接未知主机时自动将其写入 `~/.ssh/known_hosts`。如果密钥发生变动（潜在的中间人攻击），主动拦截并渲染巨型的红色警告框终止连接。
- **动态密码输入交互 (Calm Glass 风格)**：当秘钥验证失败且配置中未提供密码时，采用 GPUI 框架渲染极具现代感和主题化（冷灰渐变质感、完美居中、毛玻璃阴影）的 Calm Glass 悬浮密码输入框。输入框能够安全地拦截用户的终端按键并捕获密码，实现遮罩显示。
- **自动断线重连与超时控制**：实现了网络状态感知的会话重连机制。为 TCP 连接增加了 15 秒超时封装，彻底避免死锁挂起。连接断开时倒数并在指定时间内（5秒）循环自动重连。
- **终端本地化中文彩色反馈**：彻底淘汰了后端的英文黑盒日志，所有的前置消息（如尝试连接、验证密钥、请求密码等）已被中文化，并辅以青、绿、黄、红四色 ANSI 终端高亮，直接在终端视图上流式输出，直观清晰。
- **认证方式探测 + 密码双通道（password / keyboard-interactive）**：认证前先用 `authenticate_none` 探测服务器实际开放的认证方式并在终端打印（等价 `ssh -v` 的 "Authentications that can continue"），盲调时一眼看清。拿到密码后**同时尝试 `password` 与 `keyboard-interactive` 两种方法**（同一密码）——大量 Linux/PAM 服务器（含 WSL 自带 sshd，尤其 root）**只接受 `keyboard-interactive`**，仅发 `password` 会"密码正确仍认证失败"（OpenSSH 客户端自动回退掩盖了差异）。若服务器根本未开放任何密码类方法，错误信息会直接点名"仅支持 X，请检查 sshd_config 的 PasswordAuthentication / PermitRootLogin"。

### 2. 技术细节
- **前后端事件通信桥梁**：
  在后端的 `tn-pty/src/ssh.rs` (`PtyBackend` 的 `SshBackend`) 与前端的 `tn-ui/src/terminal_view/mod.rs` (`TerminalView`) 之间，建立了基于 `std::sync::mpsc` 与异步 waker 结合的事件通讯通道（Event Channel）。
  定义了 `PtyEvent::NeedPassword`，让异步的 SSH 协程可以中断并挂起，请求 UI 线程获取用户输入密码。UI 线程中利用了 GPUI 的 `cx.notify()` 与 `update` 打破线程壁垒，并通过 `PtyEvent` 的返回通道（reply channel）回传用户的明文密码，最终被 `russh` 进行 `authenticate_password`。
- **渲染循环与按键拦截**：
  在 `TerminalView::render` 注入悬浮输入框结构。当存在挂起的密码请求时，修改 `TerminalView::on_key`，拦截所有的终端按键流转（阻止冒泡），捕获用户的密码明文，同时支持退格和回车提交。
- **会话协程重构**：
  将 `run_session` 进行了循环包裹改造（loop）。利用 `tokio::select!` 监听来自 PTY `out_rx`、`kill_rx` 以及 `channel.wait()` 等数据流，明确区分 "网络层面断开"（自动进入 5s 延时重连）和 "应用主动要求关闭/Eof" (顺利结束线程)。

### 3. 修改的文件列表
- `d:\coder\Tn\crates\tn-pty\Cargo.toml`: 
  - 引入 `ssh2-config` 依赖，以支持配置解析。
- `d:\coder\Tn\crates\tn-pty\src\lib.rs`: 
  - 新增 `PtyEvent` 枚举 (例如 `NeedPassword` 和 `Disconnected`)。
  - 在 `PtyBackend` trait 中加入 `try_recv_event` 和 `set_waker` 接口。
- `d:\coder\Tn\crates\tn-pty\src\ssh.rs`:
  - 完善 `SshConfig::parse` 方法，调用 `ssh2_config` 获取宿主信息。
  - 在 `ClientHandler::check_server_key` 实现 `known_hosts` 匹配校验、TOFU 写入逻辑以及巨型红色警告阻断。
  - 重构 `authenticate` 方法：`authenticate_none` 探测并打印服务器认证方式；密码经 `event_tx` 请求 UI 线程并阻塞等待；**同一密码依次尝试 `authenticate_password` 与 `authenticate_keyboard_interactive_start/_respond`**（`try_keyboard_interactive` 驱动 InfoRequest 应答循环，按服务器开放方式 gate、防多余尝试触发 `MaxAuthTries`）。
  - 重构 `run_session` 为 `loop` 配合 `tokio::time::timeout` 约束，实装中文状态推送（`in_tx`），检测 `explicit_exit` 标志以自动进入断线 5 秒重试循环。
- `d:\coder\Tn\crates\tn-ui\src\terminal_view\mod.rs` & `io.rs`:
  - 将 `SshBackend` 的事件队列拉取 (`try_recv_event`) 加入每帧重绘的 `spawn_repaint_loop` 中。
  - 添加 `ssh_password_prompt` 和 `ssh_password_input` 的状态字段。
  - 在 `on_key` 内置顶密码输入的按键拦截逻辑（Enter、Backspace、Escape 等）。
  - 在 `render` 渲染树最后方加上一层 GPUI 毛玻璃浮层，绘制密码请求的遮罩层 UI。

### 4. 待实现/未实现功能

> ✅ **更新（2026-06-05）**：SSH 已**首次真机端到端验证**——Tn 以**密钥认证（id_ed25519）+ 交互 shell** 成功登录 AlmaLinux 9.7（WSL）的 sshd。认证前 `authenticate_none` 打印的"服务器支持的认证方式"与 `ssh -v` 完全吻合，正是它帮我们定位到失败是服务器侧 `PermitRootLogin no`（drop-in 覆盖主配置），而非 Tn 缺陷。以下"待实现"项是**真机仍未走过的具体路径**。

- **真机仍未走过的路径**：密码**提示框 UI**（本次走的是密钥，未触发 `NeedPassword` 浮层）；`keyboard-interactive`（测试服务器未开放该方法）；**断线重连 UX**；**TOFU 首连警告**（本次主机已在 `known_hosts`，`check_server_key` 的写入/不匹配分支未被触发）。
- **ssh-agent (`openssh-ssh-agent`) 连接**：目前考虑到 Windows 本地的 pipe 集成较为复杂且 `russh` 不提供默认支持，在此次重构中暂时跳过通过 Agent 获取私钥的功能，可以作为后续 M2 阶段进阶需求。
- **配置文件扩展解析**: 当前解析仅涉及基础的 Host、Port、User 以及单条 IdentityFile，诸如 `ProxyJump` 等更加丰富的 `~/.ssh/config` 功能集尚未被处理。
- **多次密码重试**：目前密码认证失败后会导致直接退出当前尝试。未来可以加强循环验证，允许用户多次输错密码而不断开。

## SSH 快速连接交互面板 (M2b)

### 1. 已实现功能
- **全局动态输入面板 (Quick Connect)**：在欢迎页（Welcome）、命令面板（Palette）以及分屏操作（Split）中去除了“即将支持”的占位符，落地了真实的 `LaunchRow::SshPrompt` 交互条目。
- **极致的 GPUI 视觉体验与动态居中**：面板复用了原生命令面板的“极简玻璃态（Calm Glass）”设计。更为精细的是，抛弃了单纯的全屏居中，引入了**工作区感知（Workspace-Aware）动态偏移居中**。通过读取左侧文件树（Explorer）的宽度与工作区缝隙，精确计算 `padding-left`，使得弹窗能够像磁吸一样，绝对对齐于用户当前主编辑区（欢迎页本体）的视觉正中心。
- **独立安全的焦点接管与交互闭环**：支持键盘无缝捕获，ESC 退出与 Enter 提交。在交互层面，实现了点击外围遮罩安全关闭，而点击面板本身则通过严格的事件阻断器（`stop_propagation`）配合主动焦点续签（`focus(w)` + `cx.notify()`），彻底解决了“按键穿透无反应”、“鼠标点不到面板”的焦点剥离问题。

### 2. 技术细节
- **UI 动态位置偏移计算**：
  在 `render_ssh_prompt` 渲染时，读取 `self.explorer_open` 状态和 `self.explorer_width`。当文件树展开时，通过设定外层容器的 `pl(px(ew + 23.))` 和 `pr(px(12.))`，利用 Flex 布局剩余空间划分机制，自动算出工作区的真正物理中点并垂直、水平双居中面板（代替原有的 `110px` 固定顶部悬浮）。
- **事件流与 Focus 强制保活**：
  在 `Workspace` 中保留 `ssh_prompt_focus`。为了防止 GPUI 焦点穿透至底层终端或因拦截事件导致丢焦，给输入面板本体增加独立的 `on_mouse_down`：
  ```rust
  // 核心阻断与保活逻辑
  cx.stop_propagation();
  this.ssh_prompt_focus.focus(w);
  cx.notify();
  ```
  这保证了面板对鼠标点击的百分百独占响应并维持打字光标活跃。
- **Intent 解析路由 (SshPromptIntent)**：
  利用 `SshPromptIntent` 状态机记录用户的操作来源（新建标签页 `Welcome`/`Palette` 还是分屏 `Split`）。用户按下 `Enter` 提交 `user@host:port` 时，直接实例化动态 `LaunchSpec` 交给 `spawn_pane_with`，全生命周期无缝连接 SSH 终端后台逻辑。

### 3. 修改的文件列表
- `d:\coder\Tn\crates\tn-ui\src\welcome.rs` 与 `quick_terminal.rs`:
  - 移除旧版 `SshSoon` 枚举类型，新增抛出 `SshPromptRequested` 事件，支持卡片点击拉起面板。
- `d:\coder\Tn\crates\tn-ui\src\workspace.rs`:
  - **核心渲染与位置算法**：新增 `render_ssh_prompt` 浮层组件、内联伪光标，以及基于 Explorer 宽度的弹性偏移计算 `padding`。
  - **交互保活**：实装弹窗点击事件的阻断与焦点回填代码。
  - **按键处理**：新增 `on_ssh_prompt_key` 方法，接管键盘输入，组装 `SshConfig` 结构体并发起真实连接。

## SSH 连接 UX 优化路线（设计稿 + 逐项实现）

> 站在用户角度,把 SSH 从「单行输入 + 终端里一串 ANSI 文字」升级为**有记忆、有进度、可操作**的 Calm Glass 体验。**整体设计稿**(浏览器打开即标准)= [`design/panels/06-ssh.html`](../design/panels/06-ssh.html)(已入库、评审通过);可视件遵循「design → §16 → gpui → 四守卫」链逐项端口。

### 路线总览（三轴 · A1·A2 已落地）

- **A. 交互方式**
  - **A1 最近连接 + ⭐收藏** —— ✅ **已实现**(详见下节)。
  - **A2 应用内「保存为连接」** —— ✅ **已实现**(详见下节):连接器里把一条「最近」一键命名写入 `[[profiles]]`,成为永久、可手编、跨设备携带的命名连接,打通「自由输入框 ↔ 既有 profile 系统」。
  - **A3 结构化输入 + 实时解析**:保留快捷单行;连接器已含 user/host/port 实时解析 chips,后续可加可展开分字段 + 非法标红。
  - **A4 枚举 `~/.ssh/config` 主机**:把所有 `Host` 别名做成连接器可选项(现仅查单个 host)。
- **B. 界面**
  - **B1 连接进度卡**:解析→TCP→指纹→认证(转圈)→打开 shell 分步反馈,取代/叠加终端 ANSI 文字。
  - **B2 主机指纹确认**:首连 TOFU 信任面板 + 不匹配危险卡(新旧指纹对比,默认中止),取代红字墙。
  - **B3 密码框增强**:显隐切换 + 记住密码 + **输错原地重试(不杀会话)**。
  - **B4 断线重连横幅(非模态)+ 头部/标签连接态四相点**(连接中/已连/重连/断开)。
  - **B5**:可视件先在 mockup/panels 出稿再落 gpui(铁律)。
- **C. 提示**
  - **C1 可操作错误卡**:认证失败/超时 → 原因 + [重试][改用密码][编辑连接] + 针对性提示(如 `PermitRootLogin`)。
  - **C2 拨号前校验**(空 host/坏 port 当场标红);**C3 首连引导 + 成功反馈**(显示用了密钥还是密码)。

### A1 实现明细（最近连接 + 收藏）

**目标**:成功连过的主机自动记住,连接器里**一键回连**,不必反复敲 `user@host:port`;⭐收藏置顶。

- **持久化**(`crates/tn-ui/src/ssh_recents.rs`,新模块):`SshRecent { host, user, port, auth(密钥/密码/未知), last_used, favorite }` + `SshRecents { entries }`。`record`(按 host 不分大小写 + user + port **upsert**,刷新 `last_used`、保留 favorite)· `toggle_favorite` · `sorted`(收藏优先,再按最近)· `filtered`(输入即过滤)· `trim`(收藏全留 + 最近 12 条非收藏)· `rel_time`(刚刚/N 分前/N 时前/昨天/…)。JSON 落 `%APPDATA%\Tn\ssh_recents.json`(同 `layout.rs` 范式)。**6 个 headless 单测**(upsert/排序/过滤/裁剪/round-trip/target)。
- **成功才记录(诚实)**:新增 `tn_pty::AuthKind`(PublicKey/Password/KeyboardInteractive)+ `PtyEvent::Connected { method }`;`ssh.rs::authenticate` 返回成功方法,`run_session` 在 **shell 打开后**发 `Connected`。`TerminalView::handle_pty_event` 收到后 `cx.emit(SshConnected(method))`;`Workspace` 在 `spawn_pane_with` 订阅(闭包捕获 `id`)→ 据 `pane_specs[id].ssh` 取目标 → `SshRecents::record` + `save`。失败/超时**不**记录。
- **连接器 UI**(`workspace::render_ssh_prompt` 重写):输入行 globe 图标 + 实时解析 chips;下方**最近/收藏列表**(⭐星标[点切收藏、`stop_propagation` 不触发连接] · 主机名 + `user@host:port` · 密钥/密码徽章 · 相对时间);`on_ssh_prompt_key` 支持 `↑↓` 选 / `↵` 连接选中(无匹配则连所输入) / 输入即过滤 / `Esc` 关;点行即连。空态有引导文案。连接逻辑抽成 `ssh_connect`(供输入与点选共用)。新增图标 globe/key/lock/star(`assets.rs`)。
- **顺带修复**:`spawn_pane_with` 的 cwd 继承闭包原硬索引 `self.tabs[self.active]`,在 `TN_AUTOQUIT`/`TN_DEMO`「首 pane 先于 tab push」路径越界 panic(既有潜伏 bug)→ 改 `self.tabs.get(active)` 兜底(见 CLAUDE.md 踩坑)。
- **验证**:`cargo test -p tn-ui --lib` 69 passed(含新 6 测 + 四道样式守卫)· `cargo build` 全过 · `TN_AUTOQUIT` 冒烟 exit 0。**真机肉眼**(连接器列表渲染/点选回连)待 owner。

### A2 实现明细（保存为连接 → 写 `[[profiles]]`）

**目标**:把「最近」里反复用的主机**命名**留存为永久连接(写 `config.toml` 的 `[[profiles]]`),它比 A1 的 ⭐收藏更进一层——有人类可读的**名字**、属用户自己的配置文件(可手编、可携带、`清空最近`也不丢)。

- **A1 vs A2 的分工**:A1 收藏 = 把一条最近**置顶**(轻量、仅连接器内、存 json);A2 保存 = 升级为**命名 profile**(写 `config.toml`、永久、可手编)。两者并存:连接器列表 = **「已保存」段(命名,来自 `config.toml`)在上 + 「最近」段(自动,来自 json)在下**,同端点去重(最近被对应的已保存折叠)。
- **为何走连接器而非启动磁贴**:`welcome::launch_entries` 把配置里的 `kind="ssh"` profile **折叠**进单张「SSH」启动卡(SSH 后端早期 parked 时的取舍),故 SSH profile 不会变成独立磁贴;A2 的命名连接因此显示在**连接器**(SSH 的统一入口),而非 welcome/palette 的磁贴墙。
- **写配置**(`crates/tn-config`):`profiles_toml_fragment(&[Profile])` 用 toml 序列化器吐出 `[[profiles]]` 文本片段(`None` 字段自动省略);`append_profile_to(path, &Profile)`(可测核心)+ `append_profile(&Profile)`(走 `config_path()`)把片段**追加**到文件尾、垫一空行——保留用户全部注释/格式(**别整体反序列化重写**,见 CLAUDE.md 踩坑)。**3 个 headless 单测**:片段 round-trip(含 CJK 名 + accent)、追加保留既有 `font.size` + 新 profile 解析、文件不存在时创建。
- **连接器交互**(`workspace.rs`):
  - `ssh_conn_rows()`:合并 `config.config.profiles`(SSH + 有 host,经 `split_host_port` 拆端口)与 `ssh_recents.filtered`,按输入过滤、去重,产出 owned `Vec<SshConnRow::{Saved,Recent}>`(供 `render` 与 `on_ssh_prompt_key` 共用计数/选中)。
  - 每条「最近」行加 ⊕「存为连接」按钮(`bookmark-plus` 图标,`stop_propagation` 不触发连接)→ 置 `ssh_save = Some(SshSaveDraft{ host,user,port,auth, name=host })` 进**命名子态**。
  - **命名子态**(`ssh_save.is_some()`):面板换成「保存为连接」表单(端点行 + 认证徽章 + 名称输入框);`on_ssh_prompt_key` 路由到子态——`↵` 提交 / `Esc` 返回列表 / 退格删名 / 可打印 ASCII 入名。**限制**:连接器关 IME(防中英切换干扰 host 输入),故名字暂 ASCII;要中文名可手编 `config.toml`(与命令面板中文搜索同属 IME parked 项)。
  - `ssh_save_commit`:组 `Profile{ name(空则回退 host), kind=ssh, host(端口≠22 时写 "host:port"), user(空/省略则 None), accent=None }` → `tn_config::append_profile` → 成功则 `self.config = Arc::new(tn_config::load())` 重读(连接器即现「已保存」行;不跑 `discover_profiles` 以免 `wsl.exe` 子进程)。
  - 「已保存」行:accent 圆点标识 + 名字(主)+ `user@host:port`(次)+「已保存」chip,点击即连;暂无应用内删除(手编 `config.toml`)。
- **验证**:`cargo test -p tn-config --lib` 新 3 测过 · `cargo test -p tn-ui --lib` 69 passed(四守卫含其中)· `cargo build -p tn-ui` 仅余既有 `SplitDir` 私有性 warning · `TN_AUTOQUIT` exit 0。**真机肉眼**(存为连接 → 命名 → 列表出现「已保存」段 → 回连)待 owner。**后续**:A2 之上可加应用内删除/编辑已保存连接、中文名(接 `EntityInputHandler`)、保存未连过的输入态目标。

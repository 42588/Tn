~~SSH 首次指纹 · 未保存确认 似乎没做~~

2026-06-12 复核已解决:

- SSH 首次指纹(TOFU):一直已实现 —— 没触发是因为该主机已在 `~/.ssh/known_hosts`;本轮按 SHEET 06-C 改为三按钮(仅本次信任 / 写入 known_hosts(磷光 primary)/ 取消)+ warn ⇄ 头。删掉 known_hosts 里对应行可复现样张。
- 未保存确认:已从内嵌横条改为 06-C 独立确认浮层(保存并关闭 primary / 丢弃 danger / 取消,迷你 scrim 居中)。

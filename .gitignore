/target
# Alternate target dirs (e.g. CARGO_TARGET_DIR to avoid locking a running tn.exe)
/target-*
*.pdb
*.rs.bk
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local runtime files
.claude/

# MCP tool caches (local, not shared)
.web-rooter/

# Local vendoring (root-level `cargo vendor` output only; anchored so the
# committed ConPTY redistributable under crates/tn-app/vendor/ is NOT ignored).
/vendor/

# Release artifact
tn.zip

# Installer output (cargo-packager) — lands in crates/tn-app/dist/
dist/

# Loose downloaded redistributable archives + the dev-time loose pdfium.dll at
# repo root (the real copy is vendored under crates/tn-app/vendor/pdfium/, so
# these anchored patterns must NOT match that tracked one).
*.tgz
/pdfium.dll

# Reference repos cloned locally for design study (not part of this project)
/_refs/
.playwright-cli/
tmp/
design/
.fonttmp/